# Retiring IBKR

The operator asked for IBKR to be retired from the platform (2026-09-20).
This is the plan: surveyed by eight readers over the real code, then
attacked by three — the live box, the evidence, the ordering — and **all
three refuted it**. Their corrections are folded into the stages below and
listed again at the end, because a retirement done in the wrong order does
not remove a broker: it blinds the platform to money that is still there.

> **The rule that orders everything here.** There may still be positions
> and cash at IBKR. The unknown-position sweep, `/treasury/` and the
> reconciliation are the only things that can see a lot no row claims — so
> nothing that removes the ability to SEE may run before the account is
> proved flat. Every stage before that one is reversible in a single
> command.

> **Refreshed 2026-09-23** (line numbers read at 8eddee1, 12 commits after 0e4fa38; written at 976e06d, three commits later). Treat every file:line in this document as "(line numbers drifted since 0e4fa38 — re-read before editing)" unless 8eddee1 is written beside it. The username on the VPS is Sauron, and every command runs from ~/Sauron_Vision as `./deploy/dc exec worker-fast python manage.py ...` (deploy/dc is the compose wrapper; add `-T` after `exec` when piping). Migration 0035 now exists (bot_program/migrations/0035_alter_saxoaccount_is_primary_for_stocks.py), so the forward migrations are 0036, 0037, 0038. Two orderings changed: stage 1 may only run after every open live row is CLOSED (its new gate), and a Stage 2b (move the book) now sits between stages 2 and 3. The three OPEN rows on config 10 were settled on 2026-09-23 by a measured reconcile in one process (deploy/ETORO_DEPARTURE.md §0-§1), never by writing CLOSED by hand. The measured departure plan that feeds this document is deploy/ETORO_DEPARTURE.md.

## Stage 0 — read, change nothing

```
STAGE ZERO — no code change, nothing that can lose sight of a position. All reads except step 5, which writes nothing you cannot re-derive. Run from the repo root on the VPS.

0.1 READ THE ACCOUNT AS THE PLATFORM ALREADY SEES IT. Cached columns only, opens no socket:
    ./deploy/dc exec worker-fast python manage.py treasury --user Sauron
    ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron
The first renders broker_vision.vision() — every broker row that exists, its equity with its AGE, its holdings snapshot with its age, and the divergence between the snapshot and what AssetBotTrade rows claim. The second prints env_label, host:port, gateway slot, client_id, whether a Gateway login is stored, the last manual probe, and the 2,000 USD margin-floor verdict. If the holdings snapshot reads "never read" or is hours old, you do not yet know anything about the account.

0.2 GET A FRESH ANSWER, NOT A CACHED ONE. All broker I/O lives in the sync and the sweep:
    ./deploy/dc exec worker-fast python manage.py shell -c "from bot_program.tasks import sync_broker_account; print(sync_broker_account())"
    ./deploy/dc exec worker-fast python manage.py shell -c "from django.contrib.auth.models import User; from bot_program.reconcile_asset import reconcile_unknown_positions; print(reconcile_unknown_positions(User.objects.get(username='Sauron')))"
Read the sweep dict literally. broker_unavailable > 0 or errors > 0 means UNREADABLE, not empty (reconcile_asset.py:129-132). "unclaimed: 0, broker_unavailable: 1" is not a flat account. Then re-run 0.1 and compare the ages — they must now be seconds old.

0.3 THE SEVEN FACTS NO SURVEY COULD READ FROM OUTSIDE THE BOX. One command, no writes:
    ./deploy/dc exec worker-fast python manage.py shell -c "
    from bot_program.models import IBKRAccount, AssetBotTrade, AssetBotConfig
    from bot_program.equity_models import BrokerEquityReading
    from core.models import PlatformComponent
    from market_data.models import LiveQuote, PriceData
    print('accounts', [(a.pk, a.user.username, a.host, a.port, a.env, bool(a.account_id_enc), a.has_login, a.gateway_slot, a.last_equity, a.last_equity_currency, a.last_equity_at, None if a.broker_positions is None else len(a.broker_positions), a.broker_positions_at, a.is_primary_for_stocks, a.is_primary_for_forex, a.is_primary_for_options, a.is_primary_for_commodity, a.is_primary_for_cfd) for a in IBKRAccount.objects.all()])
    print('readings total', BrokerEquityReading.objects.count(), 'ibkr', BrokerEquityReading.objects.filter(broker='ibkr').count(), 'NULL pair', BrokerEquityReading.objects.filter(account_pk__isnull=True).count(), 'still FK-bound', BrokerEquityReading.objects.filter(account__isnull=False).count())
    print('OPEN live rows stamped ibkr', list(AssetBotTrade.objects.filter(status__in=('OPEN','CLOSE_PENDING'), paper=False, metadata__broker='ibkr').values_list('id','asset_class','symbol','status')))
    print('OPEN live options/cfd rows ANY broker', list(AssetBotTrade.objects.filter(status__in=('OPEN','CLOSE_PENDING'), paper=False, asset_class__in=('options','cfd')).values_list('id','asset_class','symbol','status')))
    print('OPEN live rows with NO broker stamp', AssetBotTrade.objects.filter(status__in=('OPEN','CLOSE_PENDING'), paper=False).exclude(metadata__has_key='broker').count())
    print('closed rows stamped ibkr', AssetBotTrade.objects.filter(status='CLOSED', metadata__broker='ibkr').count())
    print('live configs that could route to ibkr', list(AssetBotConfig.objects.filter(user__username='Sauron', enabled=True).exclude(mode='paper').values_list('id','name','asset_class','mode')))
    print('ibkr quotes', LiveQuote.objects.filter(source='ibkr').count(), 'ibkr bars', PriceData.objects.filter(source='ibkr').count())
    print('components', list(PlatformComponent.objects.filter(key__in=('broker_account_sync','pipeline_asset_bots','platform_master')).values_list('key','is_enabled')))
    "
GATES this output sets, all of which must hold before the stage they gate:
  * 'NULL pair' MUST be 0 before stage 3 touches the FK. Migration 0032 backfilled the pair on every pre-2026-09-17 row; this is the only way to prove it landed on THIS database. If it is non-zero, stage 3 needs a backfill RunPython first and the plan stalls there.
  * 'OPEN live options/cfd rows ANY broker' MUST be empty before stage 4. See the hazard in stage 4's proof — it is the hardest ordering constraint in this plan.
  * 'OPEN live rows stamped ibkr' MUST be empty before stage 5.
  * 'OPEN live rows with NO broker stamp' > 0 means /treasury/ may already be under-reporting you: base.py:3202-3211 only began recording metadata["broker"] on 2026-09-19, and broker_vision.ROUTABLE_CLASSES (broker_vision.py:42) omits options and cfd, so an unstamped options row is attributed to PAPER at broker_vision.py:227 and is never compared against IBKR's holdings. If this count is non-zero, treat the divergence report in 0.1 as a floor, not a total, and cross-check by hand in the IBKR portal.
  * 'components' — if broker_account_sync is already False, every last_equity on the box is stale and "prove it flat" means the portal, not this platform. Turn it back ON before trusting 0.1.

0.4 READ THE ACCOUNT AT IBKR ITSELF, IN THE PORTAL, WITH YOUR EYES. Cash balance, every open position, every resting order, every option expiry. The database's opinion is advisory: broker_router.py:92-95 records that an empty account_id does not stop an order because TWS falls back to whichever account the session is logged into, so as long as a Gateway is signed in on port 4003 the live socket is the last line, not the row. Write the portal numbers down; they are the only independent check on 0.1.

0.5 THE ONE DIAGNOSTIC THAT CHANGES NOTHING. If anything in 0.1-0.2 read stale or unreachable:
    ./deploy/ibkr-doctor
It is read-only by construction: no restart, no recreate, no .env write. It tells apart the five causes of "IBKR unreachable" that have each cost you hours, including the one where the container is HEALTHY and the API session is gone.

0.6 DO NOT, TONIGHT OR IN ANY STAGE:
  * Do NOT press HQ Disconnect (dashboard/views_admin_hq.py:1776-1810). It clears account_id_enc, and that column is the keyed test for BOTH the unknown-position sweep (reconcile_asset.py:456-459) and the equity sync's queryset (tasks.py:341). After that click the IBKR row is not swept, not counted as broker_unavailable, and not logged — the sweep returns a confident clean pass over an account it never asked. It stops the platform SEEING the account while leaving the account funded. Nothing on this path needs it; stage 1 achieves the same de-risking without blinding anything.
  * Do NOT delete or disable the broker_account_sync PlatformComponent row (core/platform_control.py:203-205). One switch guards three tasks — sync_broker_account (tasks.py:300), sync_saxo_accounts (tasks.py:604) and sync_etoro_accounts (tasks.py:801) — and a MISSING row reads OFF, so the guarded task no-ops for ever with no log line (core/platform_control.py:68-73, graded a BLOCKER at preflight_live.py:344-346).
  * Do NOT delete or disable pipeline_asset_bots to stop the IBKR data feed. It also guards reconciliation and the sweep.
  * Do NOT register IBKRAccount in bot_program/admin.py. It is deliberately absent (bot_program/admin.py:3-6), which is why no click-path exists to a CASCADE delete of the whole equity history.
  * Do NOT run IBKRAccount.objects.all().delete(). Verified this session: bot_program/equity_models.py:31-33 is on_delete=models.CASCADE (one survey guessed SET_NULL; it is CASCADE), and tasks.py:426-428 still passes account=acct on every row, so that one statement destroys the drawdown governor's entire memory. Stage 3 exists to defuse it.
```

## Stage 1 — Stop new IBKR orders and the IBKR data feed. Delete nothing, blind nothing.

**Gate (added 2026-09-23, measured at 8eddee1).** Do NOT run this stage while any live AssetBotTrade row is OPEN or CLOSE_PENDING — on the morning of 2026-09-23 #93, #94 and #95 on config 10 were. Once the flags are unticked the router answers alpaca for a stock row (bot_program/engine/broker_router.py:268 → :52-53) and, with no Alpaca key, a PaperTrader (:318-328). An UNSTAMPED row with two venues keyed is then refused by the carrier guard on EVERY future pass (bot_program/reconcile_asset.py:70-73, call site :253) and can never again be closed with a measured proof. A row STAMPED metadata["broker"] = "ibkr" is refused for the right reason instead — "it was carried by ibkr and the router now answers paper" (reconcile_asset.py:67-69) — which is safe, logged as an ERROR every beat pass (:256-258), and still not a close. Close the rows first (stage 2 as corrected below and deploy/ETORO_DEPARTURE.md §0-§1), then untick.

**Goal.** Reduce risk to zero for new orders in stock, forex, ETF, index and commodity, and stop IBKR writing bars and quotes, while every organ that can SEE the account keeps working exactly as it did in stage zero. No file is edited, no migration is run, no service is restarted.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
No code. One database row, five booleans on the user's IBKRAccount:

    ./deploy/dc exec worker-fast python manage.py shell -c "
    from bot_program.models import IBKRAccount
    a = IBKRAccount.objects.get(user__username='Sauron')
    a.is_primary_for_stocks = a.is_primary_for_forex = a.is_primary_for_commodity = a.is_primary_for_options = a.is_primary_for_cfd = False
    a.save(update_fields=['is_primary_for_stocks','is_primary_for_forex','is_primary_for_commodity','is_primary_for_options','is_primary_for_cfd'])
    print('flags', a.is_primary_for('stock'), a.is_primary_for('forex'), a.is_primary_for('commodity'), a.is_primary_for('options'), a.is_primary_for('cfd'))
    "

WHAT THIS DOES. broker_router._ibkr_overrides (bot_program/engine/broker_router.py:123-129) is consulted on every route decision, so stock, forex, ETF, index and commodity orders stop reaching IBKR immediately and fall to their default venues (Alpaca, OANDA, paper) via _broker_for_asset_class at broker_router.py:43-58. Separately and for free, ibkr_data_feed._primary_classes (bot_program/ibkr_data_feed.py:32-45) now returns an empty set, so refresh_ibkr_data_for_user hits its documented no_primary_classes graceful-degrade and writes no further PriceData or LiveQuote rows with source="ibkr". Note is_primary_for_options shipped default=True (bot_program/models.py:156-157) — the only routing flag on any broker row that is ON at creation — so "nobody ticked anything" was never the same as "nothing routes there".

WHAT THIS DELIBERATELY DOES NOT DO. Options and CFDs still route to IBKR, because broker_router.py:268 reads `if asset_class in ("options","cfd") or _ibkr_overrides(...)` and consults no flag. That is correct for now and it is the point of this stage: it is the only path by which an open options or CFD position can still be marked, stopped, expiry-closed or flattened, since base.py:384 and options_bot.py:787 re-enter the router on every management tick. Leave the options and CFD configs ARMED until stage 2 proves them flat — an OPEN row on a disabled config is managed by nothing.
```

**Proof before the next stage.**

```
1. ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron — it must still print the IBKR row with its env_label, socket and equity (nothing was hidden), and its _venue_for (preflight_live.py:272-282) must still name ibkr for options and cfd and NOT for stock/forex/commodity.
2. ./deploy/dc exec worker-fast python manage.py treasury --user Sauron — the IBKR line must still be present with its equity and its holdings snapshot. If that line vanished, something blinded the platform and you must stop.
3. Re-run the sweep from stage 0.2 — it must still walk the IBKR row (the account branch tests account_id_enc, reconcile_asset.py:456-459, which you did not touch) and must NOT report a new broker_unavailable.
4. Confirm the feed stopped rather than broke: after one beat period, PriceData.objects.filter(source='ibkr').latest('...') must stop advancing while /health/ shows the ibkr feed as row-gated-and-idle, not as a failure.
```

**Reversible.** Fully, in one command, with no restart: set the five booleans back to their stage-0.3 recorded values. That is why stage 0.3 prints them. Nothing else in the platform changed, so there is nothing else to undo.

## Stage 2 — Prove the account flat at IBKR, and close by hand what is not.

**Goal.** Reach the state every later stage depends on: zero open positions, zero resting orders, zero cash at IBKR that you care about, and zero OPEN non-paper AssetBotTrade rows in asset_class options or cfd. Still no code change.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing; the paragraph on closing by hand was re-measured at 8eddee1 on 2026-09-23)
No code, no database edit, no service change. This stage happens at IBKR and in the platform's own close path.

CLOSE THE POSITIONS. Prefer the platform's own close, because it is still wired: options close through options_bot.submit_option_close -> market_order_option (bot_program/engine/ibkr_client.py:2031), and the stage-1 flag untick did not touch the forced options/cfd route. If you close by hand in the portal instead, the next sweep will NOT orphan-close the row when the row is unstamped and two venues are keyed — measured 2026-09-23 at 8eddee1: every TAKE TRADE row is unstamped (bot_program/manual_trade.py:1795-1806 writes protective_order_ids and protective_trade_id and no carrier; only the bot lane stamps metadata["broker"], bot_program/asset_engine/base.py:3329-3332 — one rule, AssetBot.venue_stamps, since 3ac446f), keyed_venue_count (bot_program/reconcile_asset.py:29-44) counts eToro's api_key_enc and IBKR's account_id_enc (bot_program/broker_vision.py:102-108), and unattributable() (reconcile_asset.py:47-74, called at :253) refuses the close with "it records no carrier and 2 venues are keyed" (:70-73). The measured path that closed #95 on 2026-09-23 (deploy/ETORO_DEPARTURE.md §0): log in, sync, sweep-proof, forget the eToro keys on /brokers/ so keyed == 1, then ONE shell that asserts the routed client's class is IBKRTrader (it RAISES when unreachable, bot_program/engine/ibkr_client.py:684-685, so a dead Gateway can never read as flat), prints the book, and only then calls reconcile_user — outside 13:00-21:59 UTC, because the beat reconcile (config/celery.py) has no session_busy guard and a PaperTrader handed out by the router reads [] from the simulator (bot_program/engine/paper_trader.py:175-182), which with keyed == 1 would book a false CLOSED; then re-key eToro with no class ticked. The alternative the 2026-09-23 refutation preferred, for a row whose carrier is provable: STAMP metadata["broker"] = "ibkr" (the field base.py writes, the value bot_program/engine/capabilities.py:139 defines), which turns the guard's OTHER refusal on (:67-69) — an IBKRTrader closes only on its own measured [] and any PaperTrader is refused as "carried by ibkr and the router now answers paper", at any hour, with eToro's keys untouched (tests/test_venue_drift.py pins both sides); a stamp is a provenance write by hand, so print the class and the book before the walk either way. The label manual_close is then honest — grade_bot_trade writes the outcome; the exit price is not a fill (IBKRTrader has no closing_fill — only ticker, get_positions and cancel_order) and the row carries exit_price_inferred True (reconcile_asset.py:395), or exit_price_unavailable with an UNMEASURED P&L when the venue refuses the quote, as #95 did (IBKR error 10089).

CANCEL THE RESTING ORDERS. An order is visible only to the clientId that placed it (bot_program/engine/ibkr_sessions.py:24-50), which is why ibkr_sessions must still exist at this point. Cancel through the platform or in the portal, then prove it.

DRAIN OR ACCEPT THE CASH. A funded-but-flat account is safe for every stage below; only open positions and resting orders are not. If you are also withdrawing, do that here, at IBKR, and note that below 2,000 USD IBKR itself starts refusing forex, futures and CFD margin with Error 201 (preflight_live.py:71-91) — the preflight will start shouting about a floor that no longer matters to you.
```

**Proof before the next stage.**

```
The proof is four readings agreeing, and it must be repeated at the moment you start stage 4 — not once, weeks earlier.

1. ./deploy/dc exec worker-fast python manage.py shell -c "from bot_program.tasks import sync_broker_account; print(sync_broker_account())" then ./deploy/dc exec worker-fast python manage.py treasury --user Sauron. The holdings snapshot must be SECONDS old and empty, and broker_vision.divergence() must report nothing only_broker and nothing only_platform.
2. The sweep from stage 0.2 must return unclaimed: 0 WITH broker_unavailable: 0 and errors: 0. Any non-zero in the last two means unreadable, and by reconcile_asset.py:129-132 that is not a flat account.
3. Re-run the stage 0.3 query. 'OPEN live options/cfd rows ANY broker' must be [] and 'OPEN live rows stamped ibkr' must be []. This is the gate on stages 4 and 5 and it is not negotiable — see stage 4's proof for what happens if you cut the route with one row still open.
4. The IBKR portal, with your eyes: no positions, no working orders, no pending option assignments. The live socket is the last line, not the database (broker_router.py:92-95).

Write the four results and the date somewhere you will still have them, because stage 5 removes the platform's ability to re-ask this question.
```

**Reversible.** Nothing to reverse — no platform state changed. A position closed at IBKR is of course not reversible, but that is the operator's own trade, made deliberately, not a consequence of this plan.

## Stage 2b — Move the book. Promoted 2026-09-23 from the ordering refutation; nothing after stage 2 may start until this is proved.

**Goal.** Establish that somebody other than the IBKR row is the book BEFORE stage 3 edits the history writer and long before stage 5 deletes the only writer of the IBKR reading cells. broker_backed (bot_program/capital_truth.py:179-231, read at 8eddee1) returns Saxo, then eToro, when the row is keyed AND flagged primary for one of stock/forex/commodity/crypto (:196-205, :212-222), and falls to IBKR on its key alone (:224-231). Measured 2026-09-23: the eToro row is keyed, live, holds 1.40 USD and has NO class ticked — so IBKR is still the book, and stays the book until a box is ticked on /brokers/.

**Changes.**

```
(line numbers read at 8eddee1 on 2026-09-23)
Branch (i) — a successor exists (the stated direction, eToro): tick ONE class on /brokers/ (dashboard/views_brokers.py:279-365 — the form requires both keys on every save, :291-294, and ships Demo CHECKED, templates/dashboard/brokers.html:85; untick Demo for the real account). THE BOOK MOVES ON THAT CLICK, and three things follow on the next 900 s beat (config/celery.py:437-440): every ENABLED follower is re-sized from the eToro reading in USD by bot_program/tasks.py:899-905 -> _follow_the_account (:507-596, which writes capital only, :590-591, and never base_currency; followers_of, bot_program/capital_truth.py:470-479, is enabled=True only — today that set is empty); the IBKR equity road stops being readable by both production readers (capital_truth.py:554-557 and bot_program/share_allocator.py:185-193 key on broker_backed) — which is why the export of 2026-09-23 had to happen first; and the router sends that class to eToro before IBKR (broker_router.py:262-269). The order of operations around that click — demo write proof, funding, follow, base_currency relabel, enable one at a time — is deploy/ETORO_DEPARTURE.md §4-§8.
Branch (ii) — no successor: move the removal of broker_backed's IBKR branch (capital_truth.py:224-231) OUT of optional stage 7 and INTO stage 5's commit, so broker_backed returns None and the platform reads 'not interfaced' (em dash) instead of carrying a book nobody writes to.
```

**Proof before the next stage.**

```
1. ./deploy/dc exec worker-fast python manage.py shell -c "from django.contrib.auth.models import User; from bot_program.capital_truth import broker_backed, account_equity; u=User.objects.get(username='Sauron'); b=broker_backed(u); print(type(b).__name__ if b is not None else None, account_equity(u))" — must print EtoroAccount and a reading whose age_seconds is under 900 and whose currency is USD (account_equity, capital_truth.py:258-283).
2. ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron — must NOT print the 'no equity reading has landed' blocker (preflight_live.py:579-590) for the book.
3. ./deploy/dc exec worker-fast python manage.py follow — must print `Sauron: account <value> USD, N follower(s)` with the reading (bot_program/management/commands/follow.py:129-135). The follower list is empty until §7 of ETORO_DEPARTURE.md enables one; the READING line is the proof, not the list.
```

**Reversible.** Untick the class on /brokers/ (re-entering both keys) and the book falls back to IBKR on the next call to broker_backed; the follower capitals written meanwhile are NOT restored — they are re-sized again from whichever book answers next.

## Stage 3 — Cut the 400-day equity history free of the IBKRAccount row.

**Goal.** Make the drawdown governor's entire memory survive the eventual deletion of the row it currently hangs off. This is pure safety: after this stage, no single statement anywhere can destroy the equity history. It changes no behaviour an operator can see.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
Four files and one new migration.

1. bot_program/tasks.py:425-432 — drop `account=acct` from the BrokerEquityReading.objects.create(...) call, AND drop `account=acct` from the 400-day prune's filter immediately below it, in the SAME edit. If you drop it from the writer only, the prune's filter silently stops matching and history grows without bound; if you drop it from the prune only, nothing changes. They must move together.
2. bot_program/equity_models.py:31-33 — remove the `account = ForeignKey("bot_program.IBKRAccount", on_delete=models.CASCADE, related_name="equity_readings", null=True, blank=True)` field. Verified this session: it is CASCADE, not SET_NULL, and it is the ONLY ForeignKey to IBKRAccount in the whole schema. Its related_name is read by no production code — every reader filters the (broker, account_pk) pair instead (capital_truth.py:554-556, share_allocator.py:190-193).
3. bot_program/equity_models.py:58-73 — remove the save() override in the SAME change as the FK, never before it. It is the compatibility shim that fills broker and account_pk from the FK, and its docstring records the bug it fixed: a caller passing only `account=` writes a row with NULL account_pk that no reader can see, which reads as a drawdown of zero for ever.
4. bot_program/equity_models.py:29 — change `broker = CharField(max_length=16, default="ibkr", ...)` to `default=""`. Today all three writers name their broker (tasks.py:426, 666, 869) so the default is harmless; after IBKR is gone it becomes a trap that files a real venue's reading under a dead key where no reader will ever look. Keep the field required-by-convention, not required-by-schema: a blank default is visibly wrong in a row, an "ibkr" default is invisibly wrong.
5. NEW MIGRATION bot_program/migrations/0036_*.py — RemoveField(BrokerEquityReading, "account") plus AlterField on `broker`. (0035 is taken: 0035_alter_saxoaccount_is_primary_for_stocks.py exists at 8eddee1.) Generate it, then READ it before running it. It must contain no DeleteModel and must not touch 0007, 0022, 0024, 0026 or 0032.
6. tests/test_equity_history.py lines 82, 98, 109, 117, 141, 271 — six assertions filter on `account=acct` and must move to `broker=..., account_pk=acct.pk`.

COMMAND ON THE BOX: ./deploy/dc exec worker-fast python manage.py migrate bot_program. No service restart is strictly required for a field removal, but restart web and workers anyway so no process holds the old model cache.
```

**Proof before the next stage.**

```
1. BEFORE the migration, re-confirm the gate from stage 0.3: BrokerEquityReading.objects.filter(account_pk__isnull=True).count() must be 0. If it is not, migration 0032's backfill did not land on this database and you must add a RunPython backfill to 0036 before RemoveField. Dropping the FK with NULL pairs alive orphans those rows permanently.
2. ./deploy/dc exec worker-fast python manage.py migrate bot_program --plan, read it, then run it.
3. AFTER: BrokerEquityReading.objects.count() must equal the number recorded in stage 0.3. Not approximately — exactly.
4. ONE FULL SUITE, green, before stage 4 is started.
5. ./deploy/dc exec worker-fast python manage.py treasury --user Sauron and ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron must read identically to stage 2's output — the drawdown reader still finds its history through the pair, so equity_high_water() (capital_truth.py:546-573) and drop_24h() (share_allocator.py:183-206) must return the same numbers as before the migration. If either now returns None, the pair filter is not matching and you must roll back.
```

**Reversible.** Code: git revert. Schema: the reverse migration re-adds a nullable FK, but it comes back EMPTY — Django cannot repopulate it. That is harmless, because no production reader ever read it, and the (broker, account_pk) pair carries the same information. Practically: this stage is one-way for the FK column and fully reversible for everything that reads. If you want a belt: take a database dump before the migration (./deploy/backup.sh) and keep it.

## Stage 4 — Retire IBKR as a DESTINATION. Options and CFD become paper-by-design, on purpose, with the decision written down.

**Goal.** Make the router stop sending anything to IBKR, including the two asset classes it currently sends there before consulting any flag. This is the stage the operator's request actually asks for, and it is a DECISION about options and CFDs, not a deletion: after it, this platform has no options venue and no CFD venue, and every page must say so rather than paint a red row nobody can fix.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
Six production files plus two pinned constants. All in one commit — the router and the preflight must never disagree about where an order goes.

1. bot_program/engine/broker_router.py:268 — delete the forced `asset_class in ("options","cfd")` clause and the `_ibkr_overrides(...)` clause, so nothing reaches _ibkr_client_for.
2. bot_program/engine/broker_router.py:347 — the SAME rule restated inside broker_name_for_symbol. Change it in the same edit. If the two copies disagree, a follower pool is sized from one account and traded on another (the failure tasks.py:561-565 exists to log).
3. bot_program/engine/broker_router.py:43-58 — add explicit "options" and "cfd" entries to _broker_for_asset_class returning "paper", each with a comment saying it is a decision, in the shape the existing commodity entry already uses at lines 54-57. Without this they become paper-by-accident with nothing on record.
4. bot_program/engine/broker_router.py:66-129 — remove _ibkr_client_for and _ibkr_overrides. _ibkr_overrides is read from OUTSIDE the module by its private name at broker_vision.py:204, so that call must go in this same commit or /treasury/ raises AttributeError inside a function documented as never raising (broker_vision.py:311) and the operator loses the one page that can still see IBKR money.
5. bot_program/engine/broker_router.py:137 — drop "ibkr" from VENUE_PRECEDENCE. Keep the tuple: it is read by capital_truth.broker_backed's ordering (capital_truth.py:191), preflight_live.py:268-273, views_brokers.py:222-230 and broker_vision.BROKER_ROWS ordering (broker_vision.py:32-38), and all four orderings move together. tests/test_saxo_wiring.py:153 asserts its exact value and must be updated.
6. dashboard/views_system_health.py:505 — add "options" and "cfd" to PAPER_BY_DESIGN, which today contains only {"commodity"}. Without this, check_live_mode_readiness (views_system_health.py:495-556) turns the /health/ live-broker row permanently red for every options and CFD config, printing the remedy "Add or fix broker credentials" — advice no credential can satisfy. That is how an operator learns to ignore a row, and how the next real credential failure goes unread.
7. bot_program/management/commands/preflight_live.py:272-282 and 248-251 — remove the `if asset_class in ("options","cfd") and rows["ibkr"] is not None` branch and drop "options" from _VENUE_CLASSES, so the preflight returns (None, "") for both and REFUSES to arm a live pool in them. Its own contract (preflight_live.py:255-260) says (None, "") means nothing carries this class and a live pool in it books simulated fills while calling itself live. That refusal is the rule to keep.
8. bot_program/management/commands/preflight_live.py:501-561, 71-91, 94-107 — remove the IBKR margin floor: IBKR_MARGIN_FLOOR_USD, IBKR_FLOOR_CLASSES, USD_PER_UNIT_BOUNDS, _ibkr_floor() and the whole gated block. This is IBKR's OWN rule (Error 201), not a property of money, already scoped by its own guard at preflight_live.py:505-506 and unreachable the moment broker_kind(book) stops returning "ibkr". BUT: lift the call to _foreign_currency_symbols (preflight_live.py:110-122) OUT of that block first and keep it venue-neutral — it is the platform's only currency-coverage check, a EUR account buying a USD instrument is an FX exposure at any venue, and deleting the floor silently deletes it. Also note USD_PER_UNIT_BOUNDS is the repo's only currency-bounds table; if any later work needs "roughly X in another currency", it goes with the floor.
9. bot_program/capital_truth.py:251-255 — broker_kind()'s default is "ibkr" for any unrecognised row class. Make the map explicit with NO default, returning "" for unknown. Mirror the same fix at tasks.py:766-769/795-797 (_BROKER_KINDS / _broker_kind) and it is already handled at equity_models.py:29 by stage 3. This matters beyond tidiness: preflight_live.py:505-506 gated the margin floor on broker_kind(book) == "ibkr", so an unmapped future row class would have inherited both IBKR's storage key and IBKR's margin floor.
10. bot_program/broker_vision.py:42 — ROUTABLE_CLASSES omits options and cfd, so routing() never covers them and _broker_for_row (broker_vision.py:216-228) attributes any unstamped options row to PAPER at line 227. Give the unattributable case an explicit third state rather than a fourth guess; do not simply add the classes, because routing() would then call _broker_for_asset_class and get "paper", which is the same wrong answer with more confidence.
11. bot_program/engine/broker_router.py:26-32 — SESSION_BUSY = "ibkr_session_busy" and session_busy(). Do NOT delete yet; it is still read by six consumers (base.py:401-403, 2718-2724, 3098-3104; kill_switch.py:359-374; pending_closes.py:1424-1438) and it carries a GENERAL rule — nothing was sent, so do not spend a bounded retry and do not tell the operator their broker is broken. Rename the constant and reword the operator-facing strings at base.py:2722 and base.py:3102 and kill_switch.py:369-374 so they stop naming IBKR to a human, and keep the mechanism for Saxo, whose expired-session case (broker_router.py:175-178) currently reaches none of it and therefore already spends retries the IBKR case was protected from.
12. bot_program/manual_trade.py:1933-1936 — the docstring still claims "Tracking demands an IBKR-routed class (the reading IS the IBKR account)", which the code at manual_trade.py:2081-2128 made false some time ago. Fix the prose; drop the "ibkr" entry from the venue_row map at manual_trade.py:2045-2059 but KEEP the map, because its rule (the pool would be sized from one account and traded on another) is exactly right.
13. dashboard/views_brokers.py:189-208, 222-234 — the /brokers/ page has no state for "an adapter with no route". Give the IBKR row an explicit retiring state that reads "carries nothing new" rather than green or absent.

NO MIGRATION. NO command on the box beyond restarting web and workers so the router change takes effect.
```

**Proof before the next stage.**

```
THE GATE, AND IT IS THE SHARPEST THING IN THIS PLAN. Re-run stage 0.3 and confirm 'OPEN live options/cfd rows ANY broker' is [] IMMEDIATELY before this stage, not weeks earlier. Verified this session, and it answers the question two surveys marked as unknown: reconcile_asset._broker_open_symbols (reconcile_asset.py:29-53) duck-types get_positions, and PaperTrader HAS one (bot_program/engine/paper_trader.py:175-182) returning paper BotTrade rows — so handing it a PaperTrader does NOT return None. Concretely, with one live row still open when the route is cut: a CFD row takes reconcile_asset.py:140 (`trade.symbol.upper() in state["symbols"]`) against a simulator's book, misses, and is ORPHAN-CLOSED at reconcile_asset.py:147 with outcome manual_close — while the position sits open at IBKR. An options row carrying metadata["occ_symbol"] does the same through _options_row_open_at_broker (the occ is truthy so the None guard at reconcile_asset.py:88 never fires, and it returns False). Only an options row with NO occ_symbol is safe, returning None and counting as broker_unavailable. So cutting the route with a live options or CFD row open closes it in the database, silently, while the money is still at the broker. That is the worst outcome available in this whole retirement and it is one commit away if stage 2 is not re-proved.

1. ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron — it must now refuse to arm any live options or CFD pool with (None, "") and must NOT name IBKR for any class. tests/test_preflight_venue.py walks every class against the router's own predicates so the two cannot drift; if it fails, the router and the preflight disagree and you fix that before anything else.
2. ./deploy/dc exec worker-fast python manage.py shell -c "from django.contrib.auth.models import User; from bot_program.engine.broker_router import broker_name_for_symbol as n; u=User.objects.get(username='Sauron'); print([(s, n(u, s)) for s in ('AAPL','EURUSD','GLDM')])" — no answer may be "ibkr".
3. /health/ live-broker row must be GREEN or honestly amber, never a permanent red naming options configs.
4. ./deploy/dc exec worker-fast python manage.py treasury --user Sauron — the IBKR line must STILL be there with its equity and holdings. This stage removes IBKR as a destination, not as a subject; if the treasury line disappeared, something in step 4 or 10 went too far.
5. TWO FULL SUITES expected. Update core/wall_facts.py TESTS_GREEN (line 52) only after every other edit in this stage is final, and verify it with its own run — tests/test_wall_facts.py counts the suite.
```

**Reversible.** git revert of one commit, then restart. No schema change, no data change, nothing on the box to undo. The only irreversible thing in this stage is an orphan-close that fires because the gate was skipped — which is why the gate is the first line of the proof and not the last.

## Stage 5 — Stop READING IBKR. This is the stage where the platform goes blind to the account, deliberately.

**Goal.** Remove every organ that opens a socket to IBKR: the market-data feed, the exclusive session lock, and the sync walk that writes the equity and holdings cells. After this stage the platform can no longer answer 'what is at IBKR', which is acceptable only because stage 2 proved the answer is 'nothing', stage 2b proved somebody else is the book, and stage 3 preserved the record of what it used to be.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
1. DELETE bot_program/ibkr_data_feed.py entirely (the per-user lookup at line 56, _primary_classes at 32-45, the PriceData/LiveQuote writers at 121-154). It exists only because IBKR can serve both data and execution, and every class it covered has another feed (market_data/feeds.py:94-118; market_data/bot_bars.py:76-113 guarantees a keyless path — Binance public klines for crypto, Yahoo for stocks, ETFs, indices, commodity futures and FX majors). The one thing with no substitute is options: market_data/bot_bars.py:107-108 records that options are deliberately absent from the public feed, so refresh_option_chains_for_user and current_premium_for_trade go dark and cannot be replaced from a public source. That is a consequence of stage 4's decision, not of this deletion.
2. config/celery.py:400-401 — remove the "ibkr-data-feed" beat entry, and remove the refresh_all_option_chains entry at config/celery.py:384 (its only chain source was IBKRTrader.option_chain, bot_program/engine/ibkr_client.py:1946). Also remove the now-dead beat task refresh_all_ibkr_market_data at tasks.py:235-247.
3. bot_program/tasks.py:326-345 and 425-447 — remove the IBKR walk from sync_broker_account: the is_ibkr_available() guard, the IBKRAccount.objects.exclude(account_id_enc="") queryset, the acquire_trader call at tasks.py:359-363, the five reading-cell writes, and the BrokerEquityReading create and prune. KEEP the function: it also walks Saxo (tasks.py:604) and eToro (tasks.py:801) under the same @guarded_task key.
4. bot_program/tasks.py:781-791 — _users_with_a_broker_row(): drop the IBKR term, keep the union. Its docstring records why it exists: three callers had written IBKRAccount.objects.exclude(account_id_enc="") and each silently skipped a Saxo or eToro book. Also drop the now-unused import at tasks.py:986 and the dead one at bot_program/management/commands/shares.py:153.
5. bot_program/reconcile_asset.py:450-476 and 506-522 — remove ibkr from the sweep's account-branch collection, the _CLASS/venue_name maps and the acquire_trader probe. KEEP the account branch itself: its comment (reconcile_asset.py:440-448) is the argument for its existence — a funded ISA holding hand-bought stock with no bot armed got ZERO sweeps and the function returned a confident {unclaimed: 0} from a book nobody read. Saxo and eToro still need it.
6. DELETE bot_program/engine/ibkr_sessions.py (the whole distributed lock, ibkr_sessions.py:1-550) and, IN THE SAME COMMIT, bot_program/apps.py:9-13 (the install_signal_handlers() call in ready(), verified this session — leave it and the app raises ImportError at startup) and bot_program/asset_engine/runner.py:63-64 (the explicit release_trade_sessions()). ORDER MATTERS: never remove install_signal_handlers before the module, or a worker holds the exclusive trading lease between tasks and locks the fleet out of its own orders (ibkr_sessions.py:430-438, 511-520).
7. bot_program/engine/ibkr_client.py:146-190, 276-295 — remove purpose_client_id and the CLIENT_ID_* constants (pure IBKR clientId arithmetic, read only by ibkr_sessions). Before deleting REQUEST_TIMEOUT_S (30 s, because ib_insync's default is wait-forever) and the connect backoff (_next_connect_at / CONNECT_BACKOFF_S), AUDIT saxo_client.py and etoro_client.py against both rules — they are general adapter rules learned from real incidents (a reqHistoricalData for EURUSD that never returned on 2026-09-10; bars 36 hours stale mid-week) and they should not leave the repo with IBKR.
8. bot_program/engine/ibkr_client.py:172-190 — is_ibkr_available() and its four guards (broker_router.py:81, tasks.py:331, reconcile_asset.py:477, ibkr_data_feed.py:81) go together. Never remove the function while a guard survives, or the sweep reports a clean pass over a book nobody read. Its RULE must be written into the retirement note: never cache an import failure in a module global, because the failure is a property of the calling THREAD (the first thread to touch a module in a Daphne process has no event loop) and a flag set there condemns every later request on every thread.
9. core/platform_control.py:203-205 — rename the seed row: "Broker Account Sync (IBKR)" is now simply wrong, since the same key guards the Saxo and eToro walks. Keep the KEY broker_account_sync unchanged and keep the ROW. Watch the 300-char description limit enforced on the VPS Postgres and not on the SQLite suite — a 600-char description once failed seed_components on deploy and left the stack down (core/platform_control.py:210-213).
10. dashboard/views_topology.py:91 — the broker_account_sync row declares writes = [IBKRAccount.last_equity, IBKRAccount.broker_positions, BrokerEquityReading]. Correct it to name EtoroAccount and SaxoAccount, which the same switch has always governed and which this row has always omitted. KEEP its traced note verbatim — every follower's capital, the denominator of its whole risk stack, is this reading times its share — it is the best single explanation in the repo of why the sync matters.
11. bot_program/notifications.py:712, 728, 738-742 — three IBKR defaults in one function. Keep the rule (each venue names its own remedy), make the default an explicit unknown-venue branch, and stop telling an operator to run ./deploy/ibkr-doctor for a Saxo outage.

COMMANDS ON THE BOX: ./deploy/dc exec worker-fast python manage.py seed_components (after the rename in step 9 — it is the command whose absence makes a guarded task no-op for ever, and the rename must actually land), then restart web, workers and beat so the removed beat entries stop being scheduled.
```

**Proof before the next stage.**

```
1. GATE: re-run stage 0.3 and confirm 'OPEN live rows stamped ibkr' is [] and the last treasury reading shows the account flat. This is the last moment the platform can answer that question; after this stage it cannot.
2. ./deploy/dc exec worker-fast python manage.py shell -c "from core.models import PlatformComponent; print(list(PlatformComponent.objects.filter(key='broker_account_sync').values_list('key','name','is_enabled')))" — the row must still EXIST and be enabled, with its new name. A missing row reads OFF and stops the Saxo and eToro syncs for ever with no log line (core/platform_control.py:68-73).
3. ./deploy/dc exec worker-fast python manage.py shell -c "from bot_program.tasks import sync_broker_account; print(sync_broker_account())" — must complete and must still walk Saxo and eToro. If you have no Saxo or eToro row, it returns cleanly and does nothing, which is correct.
4. ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron — must run without the IBKR block and without an ImportError. Do NOT delete preflight_live.py: bot_program/campaign_readiness.py:232-233 imports BAR_DEAD_HOURS, _bar_verdict and _market_note from it and tests/test_product_sessions.py:55-56 imports two of them. It is a shared library wearing a management command's clothes, and deleting "the IBKR preflight" takes two unrelated subsystems with it at import time.
5. ./deploy/dc exec worker-fast python manage.py treasury --user Sauron — the IBKR line will now read "never read" or age without limit. That is the expected, honest cost of this stage. Verify the Saxo and eToro lines are unaffected.
6. Confirm no process holds a lease: after a worker restart, grep the logs for any remaining ibkr_session_busy or clientId 326 message. There must be none, ever again.
7. ONE to TWO FULL SUITES. tests/test_ibkr_session_pool.py (26 tests), tests/test_trade_session_contention.py (8), tests/test_phase19_ibkr_data_feed.py and tests/test_bar_age_reads_the_market.py are deleted or rewritten here, so TESTS_GREEN in core/wall_facts.py:52 changes and needs its own verifying run, written last.
```

**Reversible.** Code: git revert, restart, and the sockets come back — the IBKRAccount row, its credentials, its gateway slot and all five reading cells are untouched by this stage, so one revert plus one sync restores full sight of the account. Data: the readings that were never taken while this stage was in force are a permanent GAP in the equity road, not a zero (tasks.py:417-419 and the BrokerEquityReading docstring are explicit that a missed sync leaves a gap and a zero row would read as a 100% drawdown). That gap is the only unrecoverable thing here, and it is why stage 2 comes first.

## Stage 6 — Stop the Gateway, remove the credential machinery, drop the dead columns. Retirement complete.

**Goal.** Take the IBKR containers off the box, remove every path by which a Gateway could be signed in again, and drop the columns that only existed to serve them. After this stage IBKR is fully retired operationally: no route, no socket, no container, no credential, no form.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
ON THE BOX FIRST, and it needs no file edit — the compose services are opt-in profiles (deploy/docker-compose.yml:249-251 profiles: ["ibkr"], and the same for ibgateway-2..5 at lines 284-351, profiles ibkr2..ibkr5):
    docker compose -f deploy/docker-compose.yml --profile ibkr --profile ibkr2 --profile ibkr3 --profile ibkr4 --profile ibkr5 stop
    docker compose -f deploy/docker-compose.yml --profile ibkr --profile ibkr2 --profile ibkr3 --profile ibkr4 --profile ibkr5 rm -f
Then prune the IBKR_* / IBKR2_* .. IBKR5_* variables from .env by hand, keeping a chmod 600 backup. Do this BEFORE the code edits below, because as long as a Gateway is logged in on port 4003 the live socket is the real authority and the database's opinion is advisory (broker_router.py:92-95).

THEN THE FILES:
1. deploy/docker-compose.yml:25-70 and 221-351 — remove the x-ibgateway anchor and all five ibgateway services. Keep the socat/relay lesson in the retirement note: the gnzsnz image binds the Gateway's own 4001/4002 to the container's loopback and relays them out as 4003 (live) and 4004 (paper), so ibgateway:4001 answers CONNECTION REFUSED for ever even after a perfect login. That cost weeks of a wrong runbook.
2. DELETE deploy/ibkr-apply and deploy/ibkr-doctor, and bot_program/management/commands/render_ibkr_env.py (the render command at lines 70-72, 118-150 is ibkr-apply's only stdout producer). PRESERVE the _env_quote() lesson somewhere in deploy/RUNBOOK.md before deleting: compose expands $ in unquoted .env values at READ time, which is how a password containing $$ reached the Gateway one $ short and was refused behind a dialog nobody could see, for three hours. That rule applies to any secret ever rendered into .env, not just IBKR's.
3. DELETE dashboard/views_admin_hq.py:464-478 and 532-700 (test_ibkr and save_ibkr_credentials) and 1776-1810's "ibkr" entry in disconnect_broker's model map; remove dashboard/urls.py:53, 55, 255, 258-259; remove the form and cards from templates/dashboard/admin_dashboard.html (the IBKR form at 554-630, the × IBKR button at 499, the badge at 492) and the IBKR references in templates/_partials/broker_account.html, templates/dashboard/brokers.html, templates/dashboard/command.html, templates/dashboard/positions_list.html, templates/dashboard/asset_bots.html, templates/base.html and templates/landing/the_wall.html. PRESERVE the blank-means-keep contract (views_admin_hq.py:568-574) as a comment on whichever credential form survives — it exists so editing a routing checkbox cannot wipe a password and take a container down at its next restart.
4. dashboard/views.py:3615, 3640, 3659-3669, 3684, 3706-3713 — remove the IBKR HQ cards, and be careful with line 3684: the comment at views.py:3636-3639 records that IBKR was once absent from this table so the page said "no broker accounts configured yet" over a configured account. Make sure an eToro-only or Saxo-only user still reads correctly after the term is dropped.
5. core/wall_facts.py:63 — drop "ibkr" from BROKER_ADAPTERS; bot_program/engine/capabilities.py:94-95 — drop the ibkr capability row; capabilities.py:124 — drop ADAPTER_CLASS_KEYS["IBKRTrader"]; tests/test_broker_contract.py:51 and 172-176 — update its own ADAPTERS map and the assertion that the account-capable set is exactly {"ibkr","etoro","saxo"}. These four move in ONE commit or the suite goes red in both directions (tests/test_broker_contract.py:68-90 confronts the published list against the capability table each way). KEEP capabilities.py:73, the options capability tier — it is the specification any replacement options venue would be measured against, and by being unclaimed it documents that the platform now has none. Fix the module docstring at capabilities.py:18-22 too, whose claim that net_liquidation/broker_portfolio exist only on IBKR has been false since etoro_client.py:326/370 and saxo_client.py:629/674, but do NOT drop its time-stop paragraph: tests/test_broker_contract.py:198-200 reads this file's source and asserts the string "time stop" appears in it.
6. DELETE bot_program/engine/ibkr_client.py, and bot_program/engine/options_bot.py's IBKR-only order path if nothing else claims it (options_bot.py:122-138, 620-633 are the only callers of market_order_option).
7. NEW MIGRATION bot_program/migrations/0037_*.py — RemoveField on the columns that now have no reader: gateway_slot, username_enc, password_enc (bot_program/models.py:195-206, their only consumer was render_ibkr_env), paper (models.py:149-152, a checkbox the form stopped submitting — views_admin_hq.py:558-565 derived it from the port instead — and safe to drop only now that the acquire_trader calls at tasks.py:363 and reconcile_asset.py:521 are gone as of stage 5), and last_balance_usd (models.py:165, documented dead in the model itself because default=0 renders an unmeasured account as an emptied one). Also remove the paper_flag_disagrees property (models.py:318-327) and the gateway_host / env_prefix properties (models.py:238-247).
8. bot_program/models.py:265-326 — the PORT RULE. This is the one place the brief's distinction bites hardest, and the answer is: the PORTS are IBKR's, the THREE-STATE CONTRACT is the platform's. PAPER_PORTS and LIVE_PORTS (models.py:278-282) are IBKR's product facts and go with them. But env / is_live / env_is_certain / env_label are the only full implementation of a rule seven other places consume through capital_truth.broker_env (whose IBKR fallthrough is getattr(acct, "env", "") at capital_truth.py:248), and the rule is stated at models.py:284-292: None is the important answer and it is NOT paper. Saxo and eToro answer the same question from sim/demo booleans, so the contract survives IBKR — but before deleting this copy, write the three-state property onto whichever row class is now the reference, or an unknown environment stops being an unknown and broker_vision.brokers() falls through to its sim/demo/live guess (broker_vision.py:167-169), which for a bare row answers "live": the wrong direction to fail.
9. bot_program/management/commands/preflight_live.py:324, 381, 385-388, 420-452 — remove the IBKR import, the row lookup and the printed block, and fix the --user help text at preflight_live.py:310-311, which still says "every user that has an IBKR account". Heed lines 385-388: this section ONCE ended in `continue` on a box with no IBKRAccount, skipping every check below it for the broker that was actually going to trade. A careless removal reintroduces exactly that shape. KEEP sections 1, 3, 4, 5 and 6 verbatim — switches, reading age, pool-vs-account, currency mismatch, one-unit-exceeds-the-ceiling, PIN — they are venue-neutral and load-bearing, and preflight_live.py:338-361 in particular is the only place on the platform that says a missing component row reads OFF out loud.
10. DELETE tests/test_ibkr_apply.py, tests/test_env_quoting.py, tests/test_ibkr_login_store.py, tests/test_ibkr_onboarding.py, tests/test_preflight_ibkr_floor.py, tests/test_phase14_options_ibkr.py.

COMMANDS ON THE BOX: the docker compose stop/rm above; ./deploy/dc exec worker-fast python manage.py migrate bot_program; ./deploy/dc exec worker-fast python manage.py seed_components (idempotent, run again after any platform_control edit); restart web, workers, beat, and re-render Caddy if any URL was removed.
```

**Proof before the next stage.**

```
1. docker compose -f deploy/docker-compose.yml ps must show no ibgateway container, and `ss -ltnp | grep -E '400[1-4]|749[67]'` on the box must return nothing. That is the proof that the live socket — the real authority — is gone.
2. ./deploy/dc exec worker-fast python manage.py check must pass, and ./deploy/dc exec worker-fast python manage.py migrate bot_program --plan must show only RemoveField operations and must not touch migrations 0007, 0010, 0021, 0022, 0024, 0025, 0026 or 0032. Those are applied on the live database and rewriting any of them breaks the dependency graph so no migration can run at all.
3. ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron, ./deploy/dc exec worker-fast python manage.py treasury --user Sauron and ./deploy/dc exec worker-fast python manage.py shares list must all run clean with no IBKR references and no ImportError.
4. Walk /health/, /brokers/, /treasury/, /portfolio/, /positions/, /shares/, /command/, /setup/, the Operations Center and the HQ page. Every one must render. Any page that cannot measure something must show an em dash, never a zero — and no page may say "no broker accounts configured" over a configured Saxo or eToro row.
5. TWO FULL SUITES: one to reach green after the test deletions, then update core/wall_facts.py:52 TESTS_GREEN and run again to verify that number, because tests/test_wall_facts.py counts the suite and the number must be written after every other edit in the stage.
```

**Reversible.** Containers: fully — `docker compose --profile ibkr up -d` brings them back, provided you kept the .env backup. Code: git revert restores every file. Schema: the reverse of 0037 re-adds the columns EMPTY, so the Gateway credentials and the slot are gone for good unless you kept a database dump — take one with ./deploy/backup.sh before migrating. The IBKRAccount row itself, its account_id_enc and all five reading cells survive this stage untouched, which is what keeps the equity history and the closed-trade attribution legible.

## Stage 7 — OPTIONAL: delete the IBKRAccount model and the last schema trace. Recommended: do not.

**Goal.** Remove the table. This is the only stage that buys nothing an operator can observe, costs the most, and destroys readability — offered because a full retirement was requested, and staged last so it can be declined without leaving anything half-done.

**Changes.**

```
(line numbers drifted since 0e4fa38 — re-read before editing)
1. market_data/feeds.py:87-91 — the "ibkr" FEEDS entry with requires_row = ("bot_program", "IBKRAccount") MUST be removed in the SAME change as the model, never after. _missing_row (feeds.py:194-204) calls apps.get_model and swallows every exception with `return ""` on the principle that an unreadable table is not evidence the feed is off; with the model gone, get_model raises LookupError, the except returns "", is_configured() returns True, and the panel reports a dead feed as switched-on-and-never-delivering — the loudest state on the panel, for a thing nobody asked for, and precisely the bug that entry's own comment says requires_row was added to fix. Also fix feeds.py:152-158 and 194-204 so a MISSING MODEL is distinguished from an unreadable table: one is a code fault, the other a deployment fault, and keep the general requires_row rule because not every feed is configured by the environment.
2. Re-point 46 test fixtures. 79 files under tests/ reference IBKRAccount and most are not IBKR tests — tests/test_sync_walks_etoro.py (8 refs), tests/test_saxo_wiring.py, tests/test_etoro_routing.py, tests/test_personas.py, tests/test_horizon.py, tests/test_monday_evening.py, tests/test_pools_are_shares_of_the_account.py, tests/test_treasury.py, tests/test_share_allocator.py, tests/test_check_feeds.py (6 refs) — create an IBKRAccount purely to obtain a book, because capital_truth.py:224-231 makes it the cheapest broker-backed row in the repo: one keyed field, no flags needed. Every one must be re-pointed at an EtoroAccount or SaxoAccount, not deleted. Budget this as the bulk of the stage and land it in ONE pass.
3. bot_program/capital_truth.py:224-231 — remove broker_backed's IBKR branch. Note the asymmetry you are deleting: Saxo (capital_truth.py:196-205) and eToro (213-222) must be keyed AND flagged primary; IBKR needed only the key. That asymmetry was load-bearing in your favour throughout stages 1-5, because it meant unticking every routing flag did not stop IBKR being the book.
4. bot_program/broker_vision.py:33-38, 96-99, 114-130, 155-176 — remove the BROKER_ROWS ibkr entry, _keyed()'s get_account_id branch and _session_line()'s IBKR answer. KEEP _reading() and _held(): they are getattr-based and venue-neutral, which is why eToro and Saxo were given columns in IBKRAccount's exact shape.
5. NEW MIGRATION bot_program/migrations/0038_*.py — DeleteModel(IBKRAccount). It can only run after stage 3's RemoveField has landed, because the FK must be gone first.
6. market_data/quotes.py:32 — leave SOURCE_PRIORITY["ibkr"] = 80 ALONE unless stage 0.3 reported zero LiveQuote and zero PriceData rows with source="ibkr". It ranks rows that already exist; removing it while they survive makes them unrankable and silently changes which mark a historical comparison used.
```

**Proof before the next stage.**

```
1. GATE: BrokerEquityReading.objects.filter(broker='ibkr').count() must equal the number recorded in stage 0.3 immediately before AND immediately after migration 0038 runs. If it drops by even one row, stage 3's RemoveField did not land and a CASCADE just destroyed the drawdown governor's memory. Take a database dump first, without exception.
2. AssetBotTrade.objects.filter(status='CLOSED', metadata__broker='ibkr').count() must be unchanged, and those rows must still render with a human-readable venue name — which is why capital_truth.py:428-429's {"ibkr": "IBKR"} label map stays for ever.
3. ./deploy/dc exec worker-fast python manage.py check_feeds (or the /health/ feeds panel) must NOT show an ibkr feed at all. If it shows one as configured, step 1 was not done in the same change and you are looking at the swallowed LookupError.
4. TWO to THREE FULL SUITES, the last one for TESTS_GREEN.
```

**Reversible.** Not meaningfully. A DeleteModel drops the table; the reverse migration recreates it empty. Everything the row knew — which socket, which account id, what the last reading was — is gone unless a dump exists. This is the only stage in the plan that cannot be undone from git, and the reason I recommend stopping at stage 6.

## What must stay readable for ever

Six things must stay readable for ever. Each is evidence, and deleting any of them does not retire IBKR — it falsifies the record of what IBKR did.

1. BrokerEquityReading, the whole table (bot_program/equity_models.py:21-77), and specifically every row carrying broker="ibkr". It is the account's road: the drawdown governor's entire memory and the verification for every graded SharePlan whose reading_value snapshot came from it. Its own docstring is explicit that a missed sync leaves a GAP and a zero row would read as a 100% drawdown. Its Meta (equity_models.py:44-56 — the (broker, account_pk, -at) index and the one_reading_per_sync_v2 constraint) is already venue-neutral and is what lets eToro and Saxo have a road at all.
   ONE HONEST QUALIFICATION, because "for ever" is not what the code does today: tasks.py:429-432 prunes anything past EQUITY_HISTORY_DAYS = 400 (tasks.py:504) on every sync, and that prune runs INSIDE the IBKR branch. So the IBKR equity road is currently being deleted at 400 days, and stage 5's removal of that branch is what actually makes it permanent. If you want more than 400 days of IBKR history preserved, the surviving rows must be dumped to a file BEFORE stage 5, because between now and stage 5 the prune is still running on every sync.

2. Every AssetBotTrade row's metadata["broker"] == "ibkr" stamp, written at bot_program/asset_engine/base.py:3202-3211 and read at broker_vision.py:224-228 and 280. It is the ONLY row-level record that a trade happened at IBKR, and the comment says why it is recorded rather than inferred: inferring it from the routing rule when the row is READ is wrong for every row opened before a flag moved. Delete the stamp, or delete the mapping that translates it, and _broker_for_row falls back to today's routing attribution — which after stage 4 resolves to paper. Past IBKR trades would then be re-attributed to a broker that never carried them. That is a falsified track record, not a lost one, and it is worse.

3. The mapping that reads that stamp back: capital_truth.py:428-429's {"saxo": "Saxo Bank", "etoro": "eToro", "ibkr": "IBKR"} label map. Without it, every historical IBKR reading and every closed IBKR trade drops to the generic "broker" on every page that renders them. Three pages once printed the literal "IBKR" over whatever book they were handed; this map is the fix, and it must outlive the venue.

4. PriceData and LiveQuote rows with source="ibkr" (written by bot_program/ibkr_data_feed.py:121-131 and 146-154), and market_data/quotes.py:32's SOURCE_PRIORITY["ibkr"] = 80 for as long as any such row survives. Those bars are the price evidence behind every backtest and every graded signal that ran on them — deleting them retro-changes the bars a stored BotBacktestRun ran on, so it can no longer be reproduced. Removing the priority entry while the rows exist makes them unrankable, which silently changes which mark a historical comparison used. Check the counts from stage 0.3 before touching either.

5. Every applied migration, untouched and unedited: bot_program/migrations/0007_ibkraccount.py, 0010, 0021, 0022, 0024, 0025, 0026_share_allocator.py and 0032_equity_reading_generic_key.py. django_migrations on the live VPS references them by name and 0008:9, 0011:11, 0023:9 and 0026:11 depend on them, so rewriting any one breaks the dependency graph and NO migration can run on that box again. Retirement adds forward migrations (0036, 0037, 0038); it never edits a past one — and 0035 (0035_alter_saxoaccount_is_primary_for_stocks.py, in the tree at 8eddee1) joins the never-edit list. 0032 in particular is the reason a safe retirement is possible at all — it backfilled the (broker, account_pk) pair onto every pre-2026-09-17 row, which is what makes stage 3's FK removal orphan nothing.

6. bot_program/models.py:743's `from .equity_models import BrokerEquityReading  # noqa: F401` re-export, because callers import the history model from bot_program.models, not from equity_models. And bot_program/models.py:380-383, 394-398, 508-513 and 526-530 — the EtoroAccount and SaxoAccount comments declaring their flags and reading cells are "in IBKRAccount's exact shape". Those comments are the written contract that makes broker_vision._reading/_held and capital_truth.broker_view work by getattr across three unrelated models; they name the reference implementation, and lines 526-530 in particular warn that env_label is read with NO guard, so a new row lacking it breaks /portfolio/ and /positions/. Keep the prose even after the referenced model is gone; reword it to say "IBKRAccount, retired, is where this shape came from".

ONE MORE THAT IS NOT DATA: do NOT register IBKRAccount in bot_program/admin.py (it is deliberately absent, admin.py:3-6). Today there is no click-path that can CASCADE-delete a row, and that absence is load-bearing for every stage of this plan.

## What the platform can still do at the end

AT THE END OF STAGE 6, venue by venue.

CRYPTO — unchanged. Binance spot and futures (broker_router.py:271-290) never touched IBKR. Public klines keep bars flowing with no credential (market_data/bot_bars.py:98-113).

FOREX — unchanged in capability, and it stops being ambiguous. OANDA carries it (broker_router.py:296-315), practice or live per the OANDA row. Yahoo covers FX-major bars keylessly. What you lose is IBKR's CASH/IDEALPRO path, which stage 1 already stopped, and the 2,000 USD margin floor blocker, which was IBKR's own rule (Error 201, preflight_live.py:71-91) and not a property of money.

STOCKS, ETFs, INDICES — unchanged. Alpaca carries execution (broker_router.py:318-330), Yahoo and Finnhub carry bars and quotes. If you were routing hand-bought ISA stock through IBKR, that is not execution — it is a holding, and after stage 5 the platform can no longer see holdings at IBKR at all.

COMMODITY / FUTURES — paper, as it already was by design. broker_router.py:54-57 says so and views_system_health.py:505's PAPER_BY_DESIGN already contains {"commodity"} so /health/ does not paint it red. IBKR's FUT path (which only worked with is_primary_for_commodity ticked) is gone. Bars still arrive from Yahoo for commodity futures.

OPTIONS — THIS IS A DECISION, NOT A DELETION, AND IT IS THE REAL PRICE OF THE REQUEST. The platform will have NO options venue. IBKR is the only adapter declaring the options capability tier (capabilities.py:73, 94-95) and the only implementation of option_chain, option_greeks and market_order_option (ibkr_client.py:1946, 2005, 2031). Concretely, after stage 4: options entries refuse loudly and correctly (options_bot.py:562-566, base.py:3093-3104); the preflight returns (None, "") and REFUSES to arm a live options pool (preflight_live.py:255-260); /health/ reads honestly because stage 4 adds options to PAPER_BY_DESIGN. After stage 5: OptionContract rows freeze because refresh_option_chains_for_user has no chain source and market_data/bot_bars.py:107-108 records that options are deliberately absent from the public feed — there is no free chain source worth trusting, so current_premium_for_trade marks stale and nothing can fix that without a new venue. The capability tier stays in the table on purpose (see never_delete): by being unclaimed it is both the written record that options execution was once possible and the contract any replacement venue would be measured against.
   AND THE THING THAT MAKES THE ORDERING NON-NEGOTIABLE: while a live options or CFD row is OPEN, the forced route at broker_router.py:268 is the only path by which it can be marked, stopped, expiry-closed or flattened, because base.py:384 and options_bot.py:787 re-enter the router on every management tick. Cut that route early and base.py:392-404 logs "cannot be managed: broker unavailable — leaving OPEN" for ever, kill_switch.py:375 raises instead of flattening, options_bot.submit_option_close raises at options_bot.py:135-138, and the EMERGENCY FLATTEN button cannot close an options row at all. Worse, and verified this session, the sweep will orphan-close it in the database — see stage 4's proof.

CFDs — same decision, same shape, no venue. Neither SaxoAccount nor EtoroAccount has a cfd flag (models.py:420-428, 536-545) and _broker_for_asset_class has no cfd case (broker_router.py:43-58), so after stage 4 CFD routes to paper explicitly and the preflight refuses to arm it live. Saxo is the obvious future home for both options and CFDs; the adapter class already exists and passes the conformance test but the ROUTER does not route to it and the sync does not walk its rows (core/wall_facts.py:66-72 records that gap by name). That is a separate piece of work, not part of this retirement.

eTORO AND SAXO — unaffected, and slightly better off. Their syncs keep running under the same broker_account_sync component (tasks.py:604, 801), their equity keeps landing in BrokerEquityReading through the (broker, account_pk) pair, they keep their own reading cells, and the drawdown governor (share_allocator.py:71-76, 319-327 — DD_KNEE 5% / DD_MAX 20% / DD_FLOOR 40%) never named a venue and is unchanged. Three fixes in this plan improve them directly: broker_kind and _broker_kind stop defaulting an unknown row class to "ibkr" (so a future venue's readings and its outage cache keys stop being filed where no reader looks), notifications stops printing IBKR remedies for a Saxo outage, and the SESSION_BUSY rule — nothing was sent, so do not spend a retry and do not tell the operator their broker is broken — gets reworded so Saxo's expired-session case can finally use it instead of spending retries the IBKR case was protected from.

WHAT STOPS WORKING IN A WAY YOU MUST ACCEPT DELIBERATELY. After stage 5 the platform has no way to answer "is there anything at IBKR". All three organs that could see money no row claims are gone: the holdings snapshot and its writer, the /treasury/ divergence row, and the unknown-position sweep's IBKR pass. broker_vision.divergence() is explicit that UNKNOWN is a first-class answer, because saying "nothing only at the broker" would be the same sentence a genuinely flat account produces (broker_vision.py:262-267) — and after stage 5 you get neither sentence, just an absent line. That is the whole reason stage 2 exists and the whole reason it cannot be skipped or dated more than a few days before stage 5 runs.

THE DRAWDOWN GOVERNOR AND THE SHARE ALLOCATOR — unchanged and still correct, reading history through the pair. The general money rules survive intact: measure a pool against the account that funds it, sum pools per venue so three pools on one account are caught, three states rather than two, and a page that cannot measure shows an em dash. The one general rule this plan must actively rescue rather than inherit is the port three-state contract — "None is the important answer and it is NOT paper" (models.py:284-292) — which exists in full only in IBKR-shaped code and must be written onto the surviving reference row before IBKR's copy is deleted, or an unknown environment stops being an unknown and broker_vision.brokers() guesses "live" for a bare row, which is the wrong direction to fail.

## Cost, honestly

Repo root: C:\Users\mathe\PycharmProjects\zz_corp\Sauron_Vision\SAURON_V\sauron_vision (branch ibkr-sessions-and-working-entries; line numbers read at HEAD 0e4fa38 — the VPS runs 976e06d, 15 commits later, on 2026-09-23; latest migration bot_program/migrations/0035_alter_saxoaccount_is_primary_for_stocks.py). The C:\Users\mathe path is the Windows checkout, not a username.

SUITE FIGURE, CORRECTED. The brief says 35 minutes. Your own operating note says 45-105 minutes for a full run, and core/wall_facts.py:52 pins TESTS_GREEN = 7551 (measured 2026-09-23 at 976e06d; it was 7342 at 0e4fa38) with tests/test_wall_facts.py counting the suite to check it. Budget 45-105 min per run and never edit the tree mid-run.

PER-STAGE COST, HONEST.
- Stage 0 (prove): 30-60 min of reading, mostly waiting on one sync and one sweep. NO suite. Zero code.
- Stage 1 (untick flags): 5 minutes, but ONLY after every live row is CLOSED (see its gate). NO suite (no file changes at all). No restart.
- Stage 2 (close at IBKR, prove flat): unbounded — it is your broker, not your code. Could be one afternoon, could be a month if an option has no liquidity. NO suite. This stage is the whole schedule risk and nothing after it can start.
- Stage 3 (cut history free of the row): 1-2 h of edits across 3 files + 1 migration + 6 test assertions. ONE FULL SUITE, mandatory — it changes a model and a writer.
- Stage 4 (retire the destination): 3-5 h. The widest stage: 6 production files plus broker_vision.ROUTABLE_CLASSES and views_system_health.PAPER_BY_DESIGN, and tests/test_saxo_wiring.py:153 and tests/test_preflight_venue.py both pin values you are changing. Budget TWO FULL SUITES — the first will find venue-table drift you cannot predict from reading.
- Stage 5 (stop reading IBKR): 2-4 h. Deletes ibkr_data_feed, ibkr_sessions, the apps.py hook, the runner release, the sync walk, 2 beat entries. Test files tests/test_ibkr_session_pool.py (26 tests), tests/test_trade_session_contention.py (8), tests/test_phase19_ibkr_data_feed.py and tests/test_bar_age_reads_the_market.py go with it. ONE to TWO FULL SUITES, and the second is the TESTS_GREEN update run.
- Stage 6 (Gateway, scripts, dead columns): 1-2 h of edits, 10 min on the box. Migration 0037. TWO FULL SUITES — one to go green after deleting tests/test_ibkr_apply.py + tests/test_env_quoting.py, one after updating TESTS_GREEN, because that number must be written after every other edit and verified by its own run.
- Stage 7 (delete the model) OPTIONAL: 1-2 working DAYS, not hours. 79 test files reference IBKRAccount and 46 create rows; most are not IBKR tests (tests/test_sync_walks_etoro.py, test_saxo_wiring.py, test_etoro_routing.py, test_personas.py, test_horizon.py, test_monday_evening.py, test_pools_are_shares_of_the_account.py all create an IBKRAccount only to obtain a book, because capital_truth.py:224-231 makes it the cheapest broker-backed row in the repo). Every one must be re-pointed at an EtoroAccount or SaxoAccount, in ONE pass, and each pass costs a full suite to check. Budget 2-3 SUITES.

TOTAL: about 2 working days plus 6-8 suite runs to reach the end of stage 6, where IBKR is fully retired operationally and the schema still carries an inert row. Stage 7 roughly doubles it and buys nothing you can see from the outside.

WHICH STAGES NEED THE SUITE: 3, 4, 5, 6, 7. Stages 0, 1, 2 and 2b touch no file and need none — that is deliberate, and it is where all the safety is.

MY RECOMMENDATION ON WHERE TO STOP: finish stage 6 and stop. An unkeyed IBKRAccount row costs one table and zero behaviour; it is the only name under which 400 days of equity readings and every closed IBKR trade remain legible, and stage 7 spends a day and a half of fixture churn to remove it. If you want the schema clean, do stage 7 as its own separate piece of work on a quiet week, never bolted onto stage 6.

## The three refutations, in full

Every one of these was a defect in the plan above, found by reading the
code rather than the plan. They are recorded verbatim because the next
person to touch this — including a later me — will be tempted by exactly
the same shortcuts. (Mechanically refreshed 2026-09-23: username, the `./deploy/dc exec worker-fast` prefix and the migration numbers +1; no argument was changed.)

### fatal — refuted: True

THREE BREAKS OF THE LIVE BOX, all of them AT OR BEFORE the plan's own recommended stopping point (end of stage 6, stage 7 declined). I read every file cited below; where the plan is right I say so at the end.

BREAK 1 — FATAL. The quote-feed registry keeps a feed whose writer stage 5 deletes, so the top-bar LIVE dot can never be green again and the daily digest mails a fault for ever. market_data/feeds.py:87-90 declares {"key":"ibkr","kind":"stream","requires":(),"requires_row":("bot_program","IBKRAccount"),"window":Window.ALWAYS,"ages":_WS}. is_configured (feeds.py:143-157) returns True: the env loop passes trivially on an empty `requires`, and _missing_row (feeds.py:160-175) returns "" because the IBKRAccount ROW still exists — stage 6 keeps it deliberately ("The IBKRAccount row itself, its account_id_enc and all five reading cells survive this stage untouched") and stage 7 is recommended declined. Stage 5 step 1 deletes bot_program/ibkr_data_feed.py, whose writers at ibkr_data_feed.py:121-131 and 146-154 are the ONLY writers of LiveQuote/PriceData source="ibkr" (verified: no other module stamps it). Window.ALWAYS means window_is_open returns True at feeds.py:196-198, so the `idle` escape at feeds.py:298-312 is unreachable. _WS = (60, 300) (feeds.py:51). So within five minutes of stage 5 the row is permanently "red" at feeds.py:315-317 (legacy source="ibkr" rows survive — never_delete point 4 insists on it), or permanently "never" at feeds.py:293-295 if there are none. Neither is in BENIGN_STATES = ("off","idle","yielding") (feeds.py:324). Consequences, none of them reversible without touching feeds.py: (a) dashboard/live_health.py:56-72 — the row enters `watched` and never `live`, so `len(live) == len(watched)` is unsatisfiable and pill_state is pinned to "yellow"/"red" on EVERY page for ever; (b) dashboard/views_system_health.py:373,393-399 — /health/ "Quote feeds" returns "fail" with "1 not delivering: IBKR (red)" and the hint "Run `python manage.py check_feeds`", advice nothing can satisfy; (c) core/component_digest.py:363-386 — _feed_faults collects state in ("never","red"), so the digest mails NOT DELIVERING: IBKR every day. feeds.py:131-138 records this exact shape as a defect already fixed once by hand: a credential-free feed with no writer "read as CONFIGURED on every deployment, sat permanently in `never`... made len(live) == len(watched) unsatisfiable. The top-bar dot could never be green on ANY install." The plan's only fix to feeds.py is stage 7 step 1 — the stage it tells the operator to decline — and stage 7's reasoning is backwards: it worries only about _missing_row swallowing LookupError once the MODEL is gone, and never notices the identical dead-but-configured state arises while model and row are PRESENT and the writer is gone. Stage 5's proof (steps 1-7) never opens /health/ at all. This is the precise harm the plan names in stage 4 step 6 — "how an operator learns to ignore a row, and how the next real credential failure goes unread" — recreated by stage 5 and left in place at the recommended stop.

BREAK 2 — FATAL, and mentioned in no stage. dashboard/views_admin_hq.py:1884-1888, inside admin_eye (def at views_admin_hq.py:1823, routed at dashboard/urls.py:211 as /admin-dashboard/eye/), reads the surviving row with NO getattr and NO try: `env, live = acct.env_label, acct.env == "live"` then `acct.host`, `acct.port`, `acct.client_id`, and `disagrees = acct.paper_flag_disagrees`. Stage 6 step 8 deletes env / is_live / env_is_certain / env_label (models.py:265-326). Stage 6 step 7 deletes paper_flag_disagrees (models.py:318-327) and the `paper` column. The row is NOT deleted (stage 6 keeps it; stage 7 declined), and the loop at views_admin_hq.py:1876-1880 reaches it for every user with `getattr(u, "ibkr_account", None)`. So the instant stage 6 lands, /admin-dashboard/eye/ raises AttributeError → 500 for ever. Stage 6's file inventory for this module (step 3) covers only lines 464-478, 532-700 and the "ibkr" entry in disconnect_broker at 1776-1810; line 1884 is outside all three. Stage 6's proof 4 says "the Operations Center and the HQ page ... Every one must render" but names neither this URL nor this line. This is the page whose own comment (views_admin_hq.py:1869-1874) says its purpose is that "an account trading real money and one trading the simulator rendered identically, which is the single distinction this page most needs to make" — so the retirement breaks exactly the surface that distinguishes real money from paper. models.py:526-530 already warns that env_label is read with no guard; the plan quotes that warning in stage 6 step 8 and then applies it only to broker_vision.

BREAK 3 — FATAL if stage 6 is applied by the line numbers it gives. The real extents in dashboard/views_admin_hq.py are: `@_admin_only` 448, `def test_ibkr_connection(request):` 449, body through `return redirect("admin_dashboard")` at 528; `@_admin_only` 530, `def save_ibkr_credentials(request):` 531, body through 706. Stage 6 step 3 says "DELETE dashboard/views_admin_hq.py:464-478 and 532-700 (test_ibkr and save_ibkr_credentials)". Deleting 532-700 leaves the decorator and a `def` with NO body at 530-531 → IndentationError at import of the module, and dashboard/urls.py:45 imports it at MODULE level (`from .views_admin_hq import (`), so EVERY URL on the platform 500s — /treasury/, /health/, /positions/, /shares/, the lot — on the one box with real money. It also orphans the tail at 701-706 (`else:` / messages.warning / return). Deleting 464-478 strips the first statements of test_ibkr_connection's body (the User import at 464 onward) and leaves 479-528 referencing `target_username`, `user` and `acct` that no longer exist, plus the surviving env_is_certain/env_label reads at 514 and 517 that stage 6 step 8 is deleting the properties for. SEPARATELY, and independent of the line numbers: stage 5 deletes bot_program/engine/ibkr_sessions.py outright, but views_admin_hq.py:489 and :616 both do `from bot_program.engine.ibkr_sessions import acquire_trader` and neither view is touched until stage 6. Stage 5's step 6 enumerates the consumers to move in the same commit — apps.py:9-13 and asset_engine/runner.py:63-64 — and omits both. Between stage 5 and stage 6 (the plan budgets 1-2 h of edits plus TWO 45-105 min suites, and forbids editing mid-run) the HQ IBKR form and its "Test connection" button are still in templates/dashboard/admin_dashboard.html, so the two POST endpoints an operator would reach for to re-probe the account raise ModuleNotFoundError → 500. Stage 5 is precisely the stage where the platform goes blind to IBKR, and it silently breaks the last manual probe path on the way.

WHAT I CHECKED AND FOUND SOUND: the FK is CASCADE, not SET_NULL (equity_models.py:31-33) and is the only FK to IBKRAccount; related_name "equity_readings" is read by no production code (only equity_models.py:33 and migrations 0026:24, 0032:78). Stage 3's paired-edit warning is correct — tasks.py:425-432 filters the prune on broker+account_pk+account, so dropping `account=acct` from the writer alone makes the prune stop matching new rows; dropping it from both leaves the pair scoping it, so Saxo (tasks.py:666) and eToro (tasks.py:869) history is not at risk. Stage 4's VENUE_PRECEDENCE claim holds: dashboard/views_brokers.py:226-227 guards with `if k in VENUE_PRECEDENCE else 99` and preflight_live.py:273 iterates the tuple, so dropping "ibkr" raises nothing. broker_vision.py:204 does read router._ibkr_overrides by its private name and the plan catches it. broker_vision.py:165 IS guarded (getattr(acct,"env_label",None)) and does fall through to the sim/demo/live guess at 167-169 answering "live" for a bare row — the plan names that correctly. The eight templates carrying IBKR strings are exactly the eight the plan lists, and admin_dashboard.html:490 ({{ row.ibkr.env_label }}) fails silently in Django, so that one is cosmetic. core/platform_control.py:203-204 and dashboard/views_topology.py:91,93 are the only component/topology rows naming IBKR, and no PlatformComponent row or beat entry is left pointing at a task the plan deletes (config/celery.py:400-401 and tasks.py:237-247 move together, and refresh_all_option_chains survives as an unscheduled task, which is harmless).

**Required changes**

- STAGE 5, NEW STEP (blocking): remove the "ibkr" entry from market_data/feeds.py:87-90 in the SAME commit that deletes bot_program/ibkr_data_feed.py. A feed whose only writer is gone must not stay declared. If the entry is kept for the record, it must be made non-alarming before the commit lands — either give it a state outside the pill (BENIGN, feeds.py:324) or drop it from FEEDS — because is_configured (feeds.py:143-157) returns True on the surviving IBKRAccount row, Window.ALWAYS blocks the idle escape (feeds.py:196-198, 298-312), and _WS=(60,300) puts it in `red` within five minutes for ever.
- STAGE 5, PROOF (new step): open /health/ and read the Quote feeds check (dashboard/views_system_health.py:373,393-399), and hit /api/live-health/ (dashboard/live_health.py:56-72) and confirm pill_state can still reach "green". Also run `./deploy/dc exec worker-fast python manage.py check_feeds` and confirm no IBKR row. Today stage 5's proof never opens a health surface at all.
- STAGE 6, NEW STEP: fix dashboard/views_admin_hq.py:1884-1888 in admin_eye (def at 1823, routed at dashboard/urls.py:211) in the SAME commit as the models.py:265-326 and 318-327 property removals. It reads acct.env_label, acct.env and acct.paper_flag_disagrees with no getattr and no try, on a row that survives stage 6 by design — drop the ibkr branch from the loop at views_admin_hq.py:1876-1880, or /admin-dashboard/eye/ 500s permanently.
- STAGE 6, STEP 3: correct the line ranges before anyone applies them. test_ibkr_connection is dashboard/views_admin_hq.py:448-528 (not 464-478) and save_ibkr_credentials is 530-706 (not 532-700). Delete whole functions including their decorators. As written, deleting 532-700 leaves `@_admin_only` + a bodyless `def save_ibkr_credentials(request):` at 530-531 → IndentationError in a module dashboard/urls.py:45 imports at module level → every URL on the box 500s.
- STAGE 5, STEP 6: add dashboard/views_admin_hq.py:489 and :616 (`from bot_program.engine.ibkr_sessions import acquire_trader`) to the list of consumers that must move in the same commit that deletes bot_program/engine/ibkr_sessions.py. Either pull both views forward into stage 5, or leave ibkr_sessions.py in place until stage 6 deletes the views. Do not ship a stage where the HQ "Test IBKR connection" and "Save IBKR credentials" buttons raise ModuleNotFoundError while their form is still rendered by templates/dashboard/admin_dashboard.html.
- STAGE 6, PROOF 4: name the URLs, not the page families. Add /admin-dashboard/eye/ and /admin-dashboard/eye/geo/ explicitly, and change "must render" to "must render with no AttributeError and no permanent red" — a page that renders a false red (break 1) passes the current wording.
- STAGE 6, PROOF: add a check that the surviving IBKRAccount row is still safe to READ from every loop that walks it. grep for every unguarded attribute read on user.ibkr_account outside the files stage 6 deletes (verified live: views_admin_hq.py:1884-1888 is one; broker_vision.py:165 and capital_truth.py:431 are correctly guarded) and confirm each one against the properties migration 0037 and stage 6 step 8 remove.
- RECOMMENDATION SECTION: the claim that stopping at stage 6 leaves "one table and zero behaviour" is false as the plan stands. At that stop the surviving row keeps market_data/feeds.py:87-90's feed switched on with no writer, and keeps views_admin_hq.py:1884 reading properties that stage 6 deleted. Either fold feeds.py:87-90 and views_admin_hq.py:1884-1888 into stage 6, or state plainly that stage 7 is mandatory rather than optional.

### needs-change — refuted: True

The plan's ordering and its safety argument survive the evidence lens. Four things do not. Two of them destroy or falsify a record the plan claims to protect, and one of its six never_delete items is protecting a table that holds no history at all.

=== 1. STAGE 3 STOPS THE *SAXO* EQUITY ROAD. Missed file. ===
The Saxo walk writes its history row with `defaults={... "account": None}` — bot_program/tasks.py:666-673, verbatim at :673. Stage 3 RemoveField's `BrokerEquityReading.account` (equity_models.py:31-33) and lists only "bot_program/tasks.py:425-432" as the writer to edit. After the migration, that get_or_create passes an unknown kwarg to the model constructor and raises TypeError — inside `except Exception as e: logger.warning("broker sync: %s (saxo) history row failed: %s", ...)` at tasks.py:674-676, whose own comment is "a history row is not the sync". The surviving broker's road stops being written and the only symptom is one warning line per 900 s beat.
It is caught by the suite: tests/test_saxo_wiring.py:305-325 runs the real task body (`_sync` at tests/test_saxo_wiring.py:103-112 calls `sync_saxo_accounts.__wrapped__.__wrapped__()`) and asserts `BrokerEquityReading.objects.get(broker="saxo", account_pk=acct.pk)` at :323-325; tests/test_saxo_end_to_end.py:263-266 does the same. So the cost is a red mandatory stage-3 suite — 45-105 min per the operating note, against the plan's "1-2 h, ONE FULL SUITE" for the whole stage.

=== 2. STAGE 3's TEST SURFACE IS 17 SITES IN 5 FILES, NOT 6 ASSERTIONS IN 1. ===
The plan names tests/test_equity_history.py:82, 98, 109, 117, 141, 271. It omits:
- tests/test_equity_history.py:49 — the `_reading()` FIXTURE, `account=acct` and nothing else (:44-50)
- tests/test_share_allocator.py:90 (fixture, `account=acct` only), :1131, :1147, :1247
- tests/test_shares_command.py:164, :179
- tests/test_shares_page.py:274, :295
- tests/test_sync_walks_etoro.py:220, :272
Those fixtures pass `account=` with NO broker and NO account_pk, so they depend on the save() shim at equity_models.py:58-73 that stage 3 deletes in the same change. The shape of the repair matters under this lens: test_share_allocator.py:90, test_shares_command.py:164 and test_shares_page.py:274 are the tests that prove the drawdown governor reads the road. Re-key them with a hardcoded `broker="ibkr"` and they go green — until stage 7 re-points the fixture at an EtoroAccount, at which point drop_24h (share_allocator.py:188-193) matches nothing, returns None, and the governor's tests pass while measuring nothing.

=== 3. THE ONLY PRUNE IN THE REPO IS INSIDE THE IBKR BRANCH — AND STAGE 5 DELETES IT. ===
`EQUITY_HISTORY_DAYS = 400` (tasks.py:504) and the single prune (tasks.py:429-432) — grep confirms two references in the tree, both there. Two consequences the plan gets half-right:
(a) Stage 3 re-keys the prune and leaves it running. Stage 2 is unbounded ("could be a month"), so the IBKR road keeps losing its oldest day on every sync for the whole of stages 0→5. Stage 3 is already editing those exact four lines; deleting the prune there is free, and it is the correction the plan's own qualification in never_delete #1 asks for by hand.
(b) Stage 5 removes it and nothing replaces it. The Saxo write (tasks.py:666-673) and the eToro write (tasks.py:862-869) never had a prune, so BrokerEquityReading grows without bound for both surviving venues and tasks.py:504 becomes dead. The plan's stage 5 step 3 lists "the BrokerEquityReading create and prune" for deletion and says nothing about this.

=== 4. STAGE 6 FALSIFIES THE DRAWDOWN READ OF THE HISTORY IT JUST PRESERVED. ===
`env` is a PROPERTY computed from the port (bot_program/models.py:282-296), not a column. Stage 6 step 8 deletes `env`/`is_live`/`env_is_certain`/`env_label` and PAPER_PORTS/LIVE_PORTS (models.py:278-296). `capital_truth.broker_env` then falls through to `getattr(acct, "env", "") or ""` (capital_truth.py:243-249) and returns "" for the IBKR row. Both readers guard on truthiness:
- equity_high_water — capital_truth.py:558-565: `env_now = broker_env(acct)` / `if env_now: rows = rows.filter(Q(env="") | Q(env=env_now))`
- drop_24h — share_allocator.py:196-200: identical shape
With env_now == "", the filter is SKIPPED and every IBKR reading is pooled regardless of world — while the IBKR row is still the book, which stages 1-6 preserve on purpose (stage 6's own reversibility note: "the IBKRAccount row itself, its account_id_enc and all five reading cells survive this stage untouched"). The readings carry `env=acct.env or ""` (tasks.py:427), so paper-port rows are stamped "paper" and are today correctly excluded. That the operator has been pointed at both worlds is the reason `paper_flag_disagrees` exists (models.py:318-327). Concretely: one IB paper NetLiquidation row inside the 90-day window beside a live reading sets the high-water mark to the simulation, drawdown reads ~99%, the governor pins to DD_FLOOR, `_shock_trigger` fires (tasks.py:445), and dashboard/oculus.py:262-271 prints the result under a qualifier that only warns about the number being 100.
The plan spotted the OTHER reader of the same property — broker_vision.py:164-167's fallthrough guess, quoted correctly in stage 6 step 8 — and missed the two money ones. And stage 0.3 prints no env histogram, so the operator cannot know whether it applies. Once the port→env map is gone, nothing can re-derive a historical row's world.

=== 5. THE REVERSE: never_delete #4 PROTECTS A TABLE WITH NO HISTORY, ON A MECHANISM THAT DOES NOT EXIST. ===
`SOURCE_PRIORITY["ibkr"] = 80` (market_data/quotes.py:32) is read by exactly one function, `_priority` at quotes.py:92, called only from `should_write` at quotes.py:95-105, called only from `write_quote` at quotes.py:123-141 — at WRITE time. No reader anywhere ranks existing rows by it. So "removing it while the rows exist makes them unrankable, which silently changes which mark a historical comparison used" (never_delete #4, repeated as stage 7 step 6) is refuted by the code.
And `LiveQuote` is a `OneToOneField(Instrument)` documented "Latest real-time quote — overwritten on each update", with `updated_at = auto_now` (market_data/models.py:31-40). One row per instrument, no history, never read by a backtest. Grouping it with PriceData as "the price evidence behind every backtest" is wrong for half the sentence. The entry's only residual effect runs backwards — a frozen source="ibkr" row at 80 would block alpaca/oanda/etoro (70) and yfinance (20) from overwriting — and only until `age > PRIORITY_HOLD_SECONDS` (300 s, quotes.py:47 and 104-105), which is permanently true once the feed stops. The stage-0.3 measurement `LiveQuote.objects.filter(source='ibkr').count()` therefore gates nothing.
PriceData is different and the plan is right about it: unique on (instrument, timeframe, timestamp), real history, and bars have no source precedence at all (market_data/models.py:8-25).

=== 6. THE ROWS ARE KEPT. THE READERS ARE NOT. THE PLAN NEVER SAYS SO. ===
Per-trade attribution: the `metadata["broker"]` stamp (asset_engine/base.py:3203-3211, via `adapter_key` at engine/capabilities.py:117-131) is rendered in exactly ONE place in the tree — templates/dashboard/treasury.html:117 `{{ p.broker }}`, fed by `broker_vision.platform_positions()`, which filters `status="OPEN"` at broker_vision.py:239-241. Stage 2 empties that set by construction. No page renders a CLOSED trade's broker; there is no other renderer. So never_delete #2's stated failure ("_broker_for_row falls back to today's routing attribution — which after stage 4 resolves to paper. Past IBKR trades would then be re-attributed") cannot fire on a closed row: `_broker_for_row` (broker_vision.py:216-232) is only ever reached for OPEN rows. Keeping the stamp is right; the argument for it is not.
never_delete #3 is weaker still: capital_truth.py:428-429's `{"ibkr": "IBKR"}` is reached only through `broker_view` → `broker_kind(broker_backed(user))` (capital_truth.py:420-433). It is an ACCOUNT-level label. It renders no historical trade and no historical reading, so preserving it keeps nothing legible.
The equity road: both readers derive the key from ONE current book — `acct = broker_backed(user)` then `filter(broker=broker_kind(acct), account_pk=acct.pk)` at capital_truth.py:553-556 and share_allocator.py:188-193. `broker_backed` returns saxo, then etoro, then ibkr (capital_truth.py:196-231). So the 400-day IBKR road becomes unreadable by every production reader the moment ANY eToro or Saxo row is keyed and flagged primary — before stage 7, before anything in this plan, and on a click. "Keep it readable for ever" is satisfied by no stage here. The export in never_delete #1 is written as a conditional ("If you want more than 400 days..."); it is unconditional.

=== WHAT I CHECKED AND FOUND SOUND ===
- equity_models.py:31-33 is on_delete=CASCADE, not SET_NULL, and grep finds it is the only FK to IBKRAccount in the schema. tasks.py:426-428 does pass `account=acct` on every row. Stage 3's existence and the "do NOT run IBKRAccount.objects.all().delete()" refusal are both correct.
- broker_router.py:268 forces options/cfd to IBKR before consulting any flag, verbatim. broker_router.py:43-58 has no options or cfd case and ends `return "paper"`, with the commodity comment at :54-57 in the shape stage 4 step 3 reuses. VENUE_PRECEDENCE = ("saxo","etoro","ibkr") at broker_router.py:137.
- views_system_health.py:505 is `PAPER_BY_DESIGN = {"commodity"}` with the comment that naming a class there is what stops a permanently red row. Stage 4 step 6 is right.
- capital_truth.py:251-255 `broker_kind` really does default every unmapped row class to "ibkr". Real trap, right to fix, and stage 4 step 9's reason (it also gated the margin floor at preflight_live.py:505-506) holds.
- models.py:156-157 `is_primary_for_options` ships default=True — the only routing flag ON at creation. Stage 1's note is accurate.
- The evidence spine is NOT broker-keyed: grep for "ibkr" across signals/, brain/, ai_agents/, portfolio/, alerts/, backtester/, instruments/, strategies/ returns zero hits. Grading keys on live/paper only (`_DECISION_VENUES`, asset_engine/aggregation.py:44-70). BotBacktestRun stores `stats` and `trades_json` snapshots (backtest_models.py:32-35), so a stored run's numbers cannot be retro-changed by deleting bars — only a re-run could differ. Graded signals, desk decisions and backtests survive this retirement untouched, which is the single largest part of the track record and the plan does not endanger it.
- The Oculus reads the road only through `budget_for` (dashboard/oculus.py:239-271) and the wall's counts are aggregates with no broker key (core/wall_facts.py:1-33, 57-75); dropping "ibkr" from BROKER_ADAPTERS moves a present-tense count 8→7 and falsifies nothing.

**Required changes**

- STAGE 3, add the missed writer: drop `"account": None` from the Saxo walk's get_or_create defaults at bot_program/tasks.py:673, in the SAME commit as the RemoveField. Otherwise that call raises TypeError inside the warning-only except at tasks.py:674-676 and the Saxo equity road stops being written.
- STAGE 3, correct the test surface from 6 sites to 17 across 5 files: tests/test_equity_history.py:49 (the `_reading` fixture, currently `account=acct` only) plus :82, 98, 109, 117, 141, 271; tests/test_share_allocator.py:90, 1131, 1147, 1247; tests/test_shares_command.py:164, 179; tests/test_shares_page.py:274, 295; tests/test_sync_walks_etoro.py:220, 272. Re-key every one to `broker=broker_kind(acct), account_pk=acct.pk` — never a hardcoded `broker="ibkr"`, or stage 7's fixture re-point makes the governor's own tests pass on zero rows.
- STAGE 3, delete the 400-day prune (bot_program/tasks.py:429-432) instead of re-keying it. It is already being edited, and leaving it alive trims the IBKR road on every sync through the whole of stage 2, whose duration the plan itself calls unbounded.
- STAGE 5, re-home the prune before deleting the IBKR branch: tasks.py:429-432 is the only prune in the repo (EQUITY_HISTORY_DAYS, tasks.py:504, has exactly two references). Add it, keyed on (broker, account_pk), to the Saxo walk at tasks.py:666-673 and the eToro walk at tasks.py:862-869 — neither has ever had one — or BrokerEquityReading grows unbounded for both surviving venues and the constant goes dead.
- STAGE 0.3, add the env histogram of the IBKR road to the gate query: `BrokerEquityReading.objects.filter(broker='ibkr').values('env').annotate(n=Count('pk'))`. The current query prints totals and the NULL pair only, and this is the measurement that decides whether stage 6 is safe.
- STAGE 6 step 8, do NOT delete `env` / `is_live` / `env_is_certain` / PAPER_PORTS / LIVE_PORTS from bot_program/models.py:278-308 while the IBKRAccount row is still the book. `env` is a property off the port, so deleting it makes capital_truth.broker_env return "" (capital_truth.py:243-249), which skips the env guard in equity_high_water (capital_truth.py:558-565) and drop_24h (share_allocator.py:196-200) and pools paper readings into the live high-water mark. Keep them (three properties and two dicts, zero behaviour — the trade already accepted for the row itself), or store env on the row and backfill it from the port in migration 0037 before the property goes. The plan's stated remedy — writing the three-state contract onto the surviving reference row — does not help, because broker_env dispatches on broker_kind and the IBKR row falls to the getattr.
- NEVER_DELETE #1, make the export unconditional and move it into STAGE 0: dump `BrokerEquityReading.objects.filter(broker='ibkr')` and `AssetBotTrade.objects.filter(status='CLOSED', metadata__broker='ibkr')` to a file before anything else. Both readers of the road derive their key from `broker_backed(user)` (capital_truth.py:553-556, share_allocator.py:188-193), which returns saxo before etoro before ibkr (capital_truth.py:196-231) — so the IBKR road goes unreadable the day any eToro or Saxo row is keyed and flagged, independently of this plan.
- NEVER_DELETE #4, split it: keep the PriceData bars with source='ibkr' (market_data/models.py:8-25 — real history, no source precedence for bars), and drop both the LiveQuote half and the SOURCE_PRIORITY['ibkr'] protection. LiveQuote is a OneToOneField on Instrument, overwritten on each update (market_data/models.py:31-40), so it holds no history; SOURCE_PRIORITY is read only by should_write at write time (market_data/quotes.py:92-105), so no historical comparison ranks by it. Remove the LiveQuote count from the stage-0.3 gate — it cannot change a decision.
- NEVER_DELETE #2 and #3, fix the stated mechanism or accept the consequence: the `metadata["broker"]` stamp is rendered only at templates/dashboard/treasury.html:117 from `platform_positions`, which filters status='OPEN' (broker_vision.py:239-241), and capital_truth.py:428-429's label map is account-scoped, not trade-scoped. No page in the tree renders a closed trade's venue. Keep the stamp, but say plainly that after stage 2 nothing in the platform can display it, and if the operator wants closed IBKR trades to read 'IBKR' anywhere, that view has to be built — it is not being preserved.

### fatal — refuted: True

THE ORDERING LENS, read against the files. Three ordering defects, one fatal.

(1) FATAL — THE BOOK NEVER MOVES, AND STAGE 5 DELETES ITS ONLY WRITER. broker_backed() returns the IBKR row on the strength of a decryptable account id alone (bot_program/capital_truth.py:223-231), while Saxo and eToro must be keyed AND flagged (capital_truth.py:196-205, 213-222). Stage 6 deliberately keeps account_id_enc and the five reading cells ("The IBKRAccount row itself, its account_id_enc and all five reading cells survive this stage untouched"), and the removal of broker_backed's IBKR branch is parked in OPTIONAL stage 7. So after stage 6 the book is still the retired row — but stage 5 deletes the only writer of last_equity/last_equity_at (bot_program/tasks.py:300-447; the reading cells are written at tasks.py:400-413 and nowhere else, as the docstring at tasks.py:302-304 states). Consequences, all in code I read: preflight_live raises a BLOCKER for a LIVE book whose reading is older than BROKER_READING_STALE_HOURS = 2.0 (preflight_live.py:43, 485-500) and broker_env(IBKR row) is acct.env = live on 4003 (capital_truth.py:233-248) — a blocker no operator action can ever clear, whose text ("the broker is not answering, whatever a stored flag says. Is the Gateway logged in?") is now false: the platform stopped asking. Every pool with extras["capital_tracks_broker"] is frozen out of new entries for ever (capital_truth.py:382-409, called on the live entry path at bot_program/asset_engine/base.py:2396-2398 and manual_trade.py:793, 1292). Pool re-sizing stops entirely, because _follow_the_account is called only inside the deleted IBKR branch (tasks.py:443-452). The plan's keeps_working section asserts "STOCKS, ETFs, INDICES — unchanged" and "FOREX — unchanged in capability": that is only true if a keyed-and-flagged Saxo or eToro row already exists, and stage 0.3 never reads SaxoAccount or EtoroAccount at all, so the proof that a successor book exists cannot be obtained from stage zero as written. This is precisely the lens's question: stage 5's precondition (somebody else is the book) is never established by any earlier stage.

(2) FATAL-ADJACENT — STAGE 2's GATE EXPIRES IN FIVE MINUTES AND STAGE 4 TAKES A DAY. Stage 1 deliberately leaves the options/CFD configs ARMED and the forced route intact (broker_router.py:268, 347). tick_all_asset_bots runs every 300 s (config/celery.py:374-375) and options entries submit real IBKR option orders (options_bot.py:558-567, 611-633, only modes are paper/live — models.py:620-622 — so nothing else blocks them). Stage 3 (1-2 h + one 45-105 min suite) and stage 4 (3-5 h + two suites) sit between the gate and the commit, so "re-run 0.3 IMMEDIATELY before this stage" cannot hold: a tick five minutes after the reading can open a new live options/CFD position, and the plan's own worst outcome then fires — reconcile_all_asset_bot_trades runs every 15 min (config/celery.py:419-420), reconcile_user builds a PaperTrader after the route is cut (broker_router.py:247-249), PaperTrader.get_positions exists (paper_trader.py:175-182), and the row is orphan-closed at reconcile_asset.py:140-151 while the money sits at IBKR. Worse than the plan says: reconcile_user has NO session_busy guard (contrast pending_closes.py:1424-1425), so ANY paper fallback — held clientId, no free slot (ibkr_sessions.py:337-342), Gateway bounced — orphan-closes live rows, and reconcile_user's queryset (reconcile_asset.py:99-101) filters neither cfg.enabled nor mode. A flat account must therefore be FROZEN flat, not merely observed once.

(3) NEEDS-CHANGE — TWO ORDERING SLIPS INSIDE THE STAGES. (a) Stage 5 deletes bot_program/engine/ibkr_sessions.py while dashboard/views_admin_hq.py:489 and :616 still import acquire_trader inside test_ibkr and save_ibkr_credentials — those views are deleted only in stage 6, so for the whole stage-5-to-6 window (hours plus 2-4 suite runs) both HQ POST paths raise ImportError and 500. (b) Stage 3 says "No service restart is strictly required for a field removal". It is required: sync_broker_account runs every 900 s (config/celery.py:429-430), and a worker still holding the pre-0036 model INSERTs account_id into a dropped column; the failure is swallowed at tasks.py:432-434 ("history row failed"), which leaves a permanent GAP in the equity road — the one thing tasks.py:417-421 and never_delete call unrecoverable.

ALSO, ON STAGE ZERO'S SUFFICIENCY — there is a way for a position to hide. The sync walks IBKRAccount.objects.exclude(account_id_enc="") (tasks.py:341) and then skips silently on `if broker_backed(user) is None: continue` (tasks.py:343-344, before attempted is incremented); the sweep's account branch collects only rows with a non-empty key (reconcile_asset.py:456-459). An already-disconnected or undecryptable row is therefore never asked, and the sweep returns unclaimed:0 / broker_unavailable:0 / errors:0 — the exact false-clean signature stage 0.6 warns about for a future HQ Disconnect. Stage 0.3 prints bool(a.account_id_enc) but no gate ever tests it, and stage 2's proof #2 is phrased as the ABSENCE of errors rather than the PRESENCE of a reading. (Credit where due: the account-entry sweep's asset_class="stock" argument at reconcile_asset.py:525 is never used in _broker_open_symbols' body (reconcile_asset.py:29-68), so that pass does see OPT positions — coverage is better there than it looks, and the age checks in 0.1/0.2 do catch the keyed case.)

WHAT I CHECKED AND FOUND SOUND: stage 1 really is inert and reversible (_ibkr_overrides at broker_router.py:123-129 is consulted per route; _primary_classes at ibkr_data_feed.py:32-45 empties the feed; the sweep's key test is untouched). Stage 4's orphan-close hazard is correctly diagnosed. Stage 4 does not fabricate live fills: options and base both refuse a live config that falls back to paper (options_bot.py:562-567, base.py:3093-3104) and management refuses too (base.py:392-404). Stage 5's ImportError warning about apps.py:9-13 and the ordering of install_signal_handlers is real. Stage 7's ordering (FK before DeleteModel, feeds.py entry in the same change) is right.

**Required changes**

- INSERT A NEW STAGE 2b — MOVE THE BOOK — before stage 5, and do not start stage 5 until it is proved. [PROMOTED into the stage list as Stage 2b on 2026-09-23.] Either (i) key a SaxoAccount or EtoroAccount and flag it primary for at least one class so broker_backed returns it (capital_truth.py:196-222), then prove account_equity(user) returns that row's reading seconds old; or (ii) if there is no successor broker, move the removal of broker_backed's IBKR branch (capital_truth.py:223-231) OUT of optional stage 7 and INTO stage 5's commit, so broker_backed returns None and the platform reads 'not interfaced' (em dash) instead of carrying a book nobody writes to.
- ADD TO STAGE 0.3 the three reads it is missing, because stage 5's precondition cannot otherwise be evaluated: SaxoAccount/EtoroAccount rows with bool(app_key_enc/api_key_enc) and every is_primary_for_* flag; capital_truth.broker_backed(user) and type(book).__name__; and the id/name of every AssetBotConfig whose extras carry capital_tracks_broker (capital_truth.py:382-388), since those pools are the ones stage 5 freezes out of new entries via base.py:2396-2398.
- ADD TO STAGE 5's PROOF, as a gate rather than an observation: ./deploy/dc exec worker-fast python manage.py preflight_live --user Sauron must NOT print the 'the book is a LIVE <kind> account and no equity reading has landed' blocker (preflight_live.py:485-500) after the stage lands. If it does, the book did not move and the stage must be rolled back.
- FREEZE FLAT, DO NOT MERELY OBSERVE IT. Add a step at the END of stage 2, immediately after the four readings agree: disable the options and CFD configs (AssetBotConfig.objects.filter(user__username='Sauron', asset_class__in=('options','cfd')).update(enabled=False)). This is safe ONLY at this point — there are no OPEN rows left for the disabled config to abandon (runner.py:38-41 skips a disabled config) — and it is what stops tick_all_asset_bots (config/celery.py:374-375) re-opening a position during stage 3 and stage 4. Do NOT disarm by setting mode='paper': client_for_symbol short-circuits to PaperTrader at broker_router.py:247-249 and reconcile_user filters neither enabled nor mode (reconcile_asset.py:99-101), so the next sweep would orphan-close any live row.
- MAKE STAGE 4's GATE AN ATOMIC PRE-DEPLOY CHECK, not a pre-stage one: the 0.3 query for 'OPEN live options/cfd rows ANY broker' must be re-run in the same shell session as the deploy/restart, after the last suite run, with the configs already disabled — and the deploy aborted if it is not []. Say explicitly that the gate is invalid if a suite run or an overnight sits between it and the restart.
- MOVE dashboard/views_admin_hq.py's test_ibkr and save_ibkr_credentials deletions (views_admin_hq.py:464-478, 532-700, including the acquire_trader imports at :489 and :616) OUT of stage 6 and INTO stage 5's commit — the same commit that deletes bot_program/engine/ibkr_sessions.py — or keep ibkr_sessions.py until stage 6. As written, both HQ POST paths raise ImportError for the whole window between the two stages.
- REWRITE STAGE 3's DEPLOY ORDER and delete the sentence 'No service restart is strictly required'. Required order: stop or pause the beat/workers (or turn broker_account_sync off for the minute, then back ON — it is one switch guarding three walks, core/platform_control.py:203-205) -> deploy the code -> ./deploy/dc exec worker-fast python manage.py migrate bot_program -> restart web, workers and beat -> turn the component back on and confirm one sync stored a row. A worker still holding the pre-0036 model INSERTs the dropped account_id column and the error is swallowed at tasks.py:432-434, leaving a permanent gap in the equity road.
- TURN STAGE 0's KEY CHECK INTO A GATE, not a printout: account_id_enc must be non-empty AND decryptable, sync_broker_account() must report attempted >= 1 and stored >= 1 (tasks.py:330 builds that dict, and tasks.py:341/343-344 are the two places a row is skipped in silence), and the sweep must report checked > 0 for the IBKR row. State that 'unclaimed: 0, broker_unavailable: 0, errors: 0' on an UNKEYED row is the false-clean signature of stage 0.6, not a flat account, and that only a broker_positions_at seconds old proves the account was asked.
- MOVE THE GATEWAY SHUTDOWN EARLIER. The compose stop/rm in stage 6 needs no file edit (deploy/docker-compose.yml:249-251 profiles), so run it at the START of stage 4 — after the flat re-proof and while the platform can still read the account — rather than after stage 5 has blinded it. Otherwise a funded, signed-in live socket on 4003 sits for the whole stage-5-to-6 window with nothing able to see it. Then reword stage 4's proof #4: the treasury IBKR line must still be PRESENT with an honest age, not 'with its equity and holdings', or the proof contradicts the step.
