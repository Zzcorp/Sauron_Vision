# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.eggs/
*.so

# Virtual environments
venv/
.venv/
env/

# Django
*.log
local_settings.py
db.sqlite3
# Leftover clones from `manage.py test --parallel` runs killed mid-flight —
# the runner names them default_<n> and cannot clean up after a crash.
default_*.sqlite3
media/
staticfiles/

# Environment
.env
.env.local
.env.production

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (frontend)
node_modules/
frontend/build/
frontend/dist/

# Celery
celerybeat-schedule
celerybeat.pid

# Claude Code local state
.claude/

# Operator screenshots dropped locally — never ship into the public static root
static/screen/
.env.bak
.env.tmp
.env.lock
